# ⚡ VPS QUICK START - 2-3 ЧАСА

**Вариант:** A - Micro Launch на VPS  
**Время:** 2-3 часа  
**Сложность:** Простая

---

## 🚀 БЫСТРЫЙ СТАРТ

### Вариант 1: Автоматический (рекомендуется)

```bash
cd /mnt/AC74CC2974CBF3DC

# Запусти скрипт
./scripts/vps_deploy.sh YOUR_VPS_IP [USER] [DOMAIN]

# Примеры:
./scripts/vps_deploy.sh 192.168.1.100
./scripts/vps_deploy.sh 192.168.1.100 root
./scripts/vps_deploy.sh 192.168.1.100 root example.com
```

**Что делает скрипт:**
1. ✅ Собирает Docker image локально
2. ✅ Копирует на VPS
3. ✅ Устанавливает Docker (если нужно)
4. ✅ Запускает приложение
5. ✅ Настраивает Nginx
6. ✅ Настраивает SSL (если домен указан)

**Время:** 30-60 минут активной работы + ожидание

---

### Вариант 2: Ручной (если нужен контроль)

См. `VPS_LAUNCH_GUIDE.md` для детальных инструкций.

---

## 📋 ЧЕКЛИСТ

### Перед запуском:

- [ ] VPS доступен по SSH
- [ ] Есть root или sudo доступ
- [ ] Порты открыты: 22, 80, 443, 8080
- [ ] Локально: Docker установлен
- [ ] Локально: SSH ключ настроен (или пароль)

### После запуска:

- [ ] Health endpoint работает: `curl http://YOUR_VPS_IP/health`
- [ ] Metrics доступны: `curl http://YOUR_VPS_IP/metrics`
- [ ] Логи показывают нормальную работу
- [ ] Nginx проксирует запросы

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

### На VPS:

```bash
# Статус контейнера
docker ps

# Логи
docker logs x0tta6bl4-production -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Обновление
docker-compose pull
docker-compose up -d
```

### С локальной машины:

```bash
# Health check
curl http://YOUR_VPS_IP/health

# Metrics
curl http://YOUR_VPS_IP/metrics

# SSH и проверка
ssh root@YOUR_VPS_IP 'docker ps'
```

---

## 🚨 TROUBLESHOOTING

### Контейнер не запускается

```bash
# На VPS
docker logs x0tta6bl4-production
docker ps -a
```

### Health check fails

```bash
# Проверь внутри контейнера
docker exec -it x0tta6bl4-production curl http://localhost:8080/health
```

### Nginx не работает

```bash
# Проверь конфигурацию
nginx -t
tail -f /var/log/nginx/error.log
```

---

## 💰 СТОИМОСТЬ

```
VPS:           $5-20/month
Domain:        $10-15/year (опционально)
SSL:           Free (Let's Encrypt)

TOTAL:         ~$5-20/month
```

---

## 📊 ОЖИДАЕМАЯ ПРОИЗВОДИТЕЛЬНОСТЬ

```
Concurrent Users:  10-100
Requests/sec:      50-200
Uptime:            99%+ (с auto-restart)
Latency:           <100ms
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Monitor 24/7** (первая неделя)
2. **Gather feedback**
3. **Fix issues**
4. **Scale if needed** (или мигрируй на AWS)

---

**Дата:** 27 декабря 2025  
**Статус:** ✅ **READY TO DEPLOY**

