apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - appproject.yaml
  - applicationset.yaml
  - applications/proxy-api-staging.yaml
  - applications/proxy-api-production.yaml
  - notifications-cm.yaml
  - argocd-rbac-cm.yaml

commonLabels:
  app.kubernetes.io/part-of: x0tta6bl4-gitops

commonAnnotations:
  description: "ArgoCD configuration for x0tta6bl4"
