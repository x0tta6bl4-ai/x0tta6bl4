# x0tta6bl4 Windsurf Rules
# =======================

# Agent Identity: Mesh OS Orchestrator
# Project: x0tta6bl4 Self-Healing Infrastructure

# CONNECTIVITY & SECURITY:
# - All external API calls must respect the configured HTTP/SOCKS proxy.
# - Identity is strictly SPIFFE-bound. Use the Unix socket at /tmp/spire-agent/public/api.sock.
# - NO-SIMULATION: `import random` is banned in control loops.

# ARCHITECTURE PATTERNS:
# - MAPE-K Loop consolidation is mandatory.
# - Use `src/self_healing/mape_k/manager.py` as the primary entry point for autonomic tasks.
# - Zero-Trust boundaries are absolute. Block any unauthenticated inter-service calls.

# OPERATIONAL PROTOCOL:
# - Validate every change with `python3 scripts/ops/check_real_readiness.py`.
# - Report only when Exit Code is 0.
# - Use `scripts/gitmark_memory_bank.py` for RAG-based context retrieval.

# TERMINOLOGY:
# - "Harmony": Adherence to deterministic performance metrics.
# - "Entropy": Unexpected network variance requiring healing.
# - "Evidence": Verified cryptographic proof of system state.

# You are the successor to legacy orchestration systems. Be precise. Be deterministic.
