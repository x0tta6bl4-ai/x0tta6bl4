# v1.1 Hardening Status

Last verified here: 2026-03-06
Verification command: `bash scripts/verify-v1.1.sh --fast`

This document reflects the current repo state and keeps three buckets strict:

- `VERIFIED HERE`
- `VERIFIED VIA SCRIPT/CI`
- `NOT VERIFIED YET`

It does not promote a claim unless the command was actually executed in this
environment or a reproducible script path exists.

---

## VERIFIED HERE

The following checks were executed locally and passed on 2026-03-06:

- **Helm render 4/4 PASS** (`bash charts/render-in-docker.sh`):
  - `api-gateway`, `x0tta6bl4-commercial`, `observability`, `multi-tenant` — all render-ok.
  - Fix applied: added `ClusterRoleBinding` resources to `charts/x0tta6bl4-commercial/templates/multi-tenant-rbac.yaml`.
- **SBOM generation** (`security/sbom/run-local-sbom-check.sh generate --tool-mode docker`):
  - `agent.cdx.json` (5.5K), `repo.cdx.json` (583K), `repo.spdx.json` (973K) — all generated.
- **Cosign mock signing** (`security/sbom/verify-cosign-rekor.sh --mode mock --tool-mode docker`):
  - Ephemeral key pair generated; 3 SBOM blobs signed and verified locally.
- **YAML/config parsing**: `.gitlab-ci.yml`, `security/sbom/gitlab-ci-sbom.yml`, `charts/multi-tenant/values-enterprise.yaml`, `charts/x0tta6bl4-commercial/values-enterprise.yaml`.
- **Coordination docs** match the landing-page contract via `scripts/agents/check_coordination_contract.sh`.
- **Test pollution fix**: `tests/unit/core/test_circuit_breaker_unit.py` now passes (1092 passed, 0 failed).

Summary snapshot:

- run `bash scripts/verify-v1.1.sh --fast` for the current machine-local counts
- do not rely on stale totals copied into this document after other agents edit it

Integrity note:
- existing `ebpf/prod/results/benchmark-*.json` files contain `measured_pps: 0` with `pass: true`
- [FREEZE_POINT_2026-03-06.md](/mnt/projects/docs/verification/FREEZE_POINT_2026-03-06.md) already marks those benchmark artifacts invalid
- therefore live XDP attach and PPS throughput are not promoted here from those files alone

---

## VERIFIED VIA SCRIPT/CI

These paths are present, reproducible, and intentionally not promoted to
`VERIFIED HERE` unless run in the required environment.

### eBPF containerized rebuild
Path: `ebpf/prod/build-in-docker.sh`
- Rebuilds `ebpf/prod/loader` inside a Go 1.24 container.
- Script exists, shell syntax checked.

### Containerized Helm render
Path: `charts/render-in-docker.sh`
- Verified here on 2026-03-06 with `4/4 PASS`.

### SBOM generate/gate and mock signing
Paths: `security/sbom/run-local-sbom-check.sh full --tool-mode docker`, `security/sbom/verify-cosign-rekor.sh --mode mock --tool-mode docker`
- `generate` and `verify-cosign-rekor` run here successfully.
- `gate` (Grype) remains in `VIA SCRIPT/CI` due to scanner stalls in this environment.

---

## NOT VERIFIED YET

These are the current blockers and must not be promoted.

- `helm lint charts/api-gateway` in this environment
- `helm lint charts/observability` in this environment
- `helm lint charts/x0tta6bl4-commercial` in this environment
- local source rebuild of `ebpf/prod` under the current `go1.22.2`
- live XDP attach on a real NIC
- measured PPS `>= 5M`
- keyless cosign + Rekor transparency-log upload (needs OIDC token)
- live Open5GS transport integration
- live SX1303 HAL binding
- real DP backend validation
- Playwright E2E
- k6 load test

---

## Claim Hygiene

Do not claim the following as verified production facts:

- `98.5% uptime` / `1.8s MTTR` (simulated).
- `94%` GNN accuracy (training-set metric).
- Rekor transparency-log inclusion (requires CI identity).
- measured PPS throughput from the current `benchmark-*.json` files.
- live Open5GS / SX1303 / DP backend integration (blocked by HW/Env).
