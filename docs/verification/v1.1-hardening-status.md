# v1.1 Hardening Status

Last verified here: 2026-03-06  
Verification command: `bash scripts/verify-v1.1.sh --fast`

This document reflects the current repo-grounded evidence state and keeps three
buckets strict:

- `VERIFIED HERE`
- `VERIFIED VIA SCRIPT/CI`
- `NOT VERIFIED YET`

Do not promote live claims from prose alone. If a claim requires a machine-
readable artifact, the artifact wins.

---

## VERIFIED HERE

The following checks were executed locally and passed in this environment:

- `.gitlab-ci.yml` parses
- `security/sbom/gitlab-ci-sbom.yml` parses
- `charts/multi-tenant/values-enterprise.yaml` parses
- `charts/x0tta6bl4-commercial/values-enterprise.yaml` parses
- CO-RE generation path is wired (`make -n`)
- Kernel is `>= 6.1`
- BTF is present at `/sys/kernel/btf/vmlinux`
- Coordination docs do not drift back to manual status-board mode
- `compliance/soc2/playbook.md` contains SEV-1 definition
- `compliance/soc2/evidence-matrix.md` exists
- `docs/verification/v1.1-hardening-status.md` exists
- `docs/verification/operator-live-validation-checklist.md` exists
- `ebpf/prod/benchmark-harness.sh` plan-only path exits successfully

Current local snapshot from `scripts/verify-v1.1.sh --fast`:

- `VERIFIED HERE: 13`
- `VERIFIED VIA SCRIPT/CI: 1`
- `NOT VERIFIED YET: 14`
- `FAILED: 0`

Additional operator-verified local checks on 2026-03-06 (not counted inside the
`--fast` totals above):

- `bash charts/render-in-docker.sh` -> `Passed: 4 | Failed: 0`
- `bash security/sbom/run-local-sbom-check.sh full --tool-mode docker` ->
  completed; CycloneDX/SPDX SBOMs plus `*.grype.json` artifacts produced under
  `security/sbom/out/`
- `bash security/sbom/verify-cosign-rekor.sh --mode mock --tool-mode docker` ->
  local mock blob signing/verification completed; `rekor=skipped`
- `env -u GOSUMDB GOCACHE=/mnt/projects/.tmp/go-build go test ./edge/5g/... -v`
  -> `ok x0tta6bl4/edge/5g`
- `bash scripts/verify-5g-path.sh` -> targeted 5G verification harness
  completed without promoting live Open5GS/XDP status
- `pytest -q tests/unit/api/test_maas_negative_cases_unit.py tests/unit/dao/test_governance_dispatch_unit.py tests/unit/dao/test_governance_script_unit.py tests/unit/dao/test_incident_workflow.py --no-cov`
  -> `78 passed in 15.63s`
- `bash -n scripts/ops/mesh_operator_webhook_admission_e2e.sh` -> shell syntax ok
- `docs/release/MESH_OPERATOR_RELEASE_DRY_RUN_LATEST.md` matches
  `docs/release/mesh_operator_release_dry_run_20260306T190608Z.md`; the paired
  JSON artifact confirms `CP-01..CP-10 PASS`

---

## VERIFIED VIA SCRIPT/CI

- `ebpf/prod/build-in-docker.sh` provides the containerized build path for the
  nested eBPF module when the local Go toolchain is older than the module
  requirement.

---

## NOT VERIFIED YET

- Helm lint in the reproducible sandboxed local path; use
  `charts/render-in-docker.sh`
- `go build ./ebpf/prod` with the current local Go toolchain
- Python unit tests in the `--fast` verification profile
- Docker-backed checks when Docker is unreachable in the current execution
  context
- Live XDP attach on a real NIC
- PPS benchmark `>= 5M`
- Keyless cosign + Rekor transparency-log upload
- Live Open5GS UPF session against a real endpoint
- Live SX1303 HAL binding
- Real DP backend validation
- Playwright E2E
- k6 load test

---

## Integrity Flags

- The historical `8.8M PPS` claim remains **NOT VERIFIED**.
- The current repo-local benchmark JSON files under `ebpf/prod/results/` still
  record `measured_pps=0` while showing `"pass": true`; they are not valid gate
  artifacts for PPS promotion.
- Do not promote any PPS number without a fresh `benchmark-<ts>.json` generated
  from a real `pktgen`/physical-NIC run.
- Do not promote Rekor, live XDP, Open5GS, or k6 load status without the
  required machine-readable evidence listed in
  `docs/verification/operator-live-validation-checklist.md`.
