# v1.1 Hardening Status

Last verified here: 2026-04-15
Verification command: `bash scripts/verify-v1.1.sh --fast`
Cascade audit: rate limiting + xray health checks added to /json and /sub; unit test drift fixed; eBPF .o artifacts removed; CHANGELOG created for sync 1bfe971b

This document reflects the current repo-grounded evidence state and keeps three
buckets strict:

- `VERIFIED HERE`
- `VERIFIED VIA SCRIPT/CI`
- `NOT VERIFIED YET`

Do not promote live claims from prose alone. If a claim requires a machine-
readable artifact, the artifact wins.

Current release posture is governed by `docs/verification/release-gate-v1.1.md`.
Historical 2026-03-08 CI/VPS notes below are retained for context only and do not override the current release gate.

---

## Canonical commands

- Verification baseline: `bash scripts/verify-v1.1.sh --fast`
- Readiness snapshot: `python3 scripts/ops/readiness_snapshot.py --refresh`
- Canonical operating model: `docs/verification/CANONICAL_OPERATING_MODEL.md`

Use those three entrypoints before relying on older notes, archived reports, or
manual CI references.

---

## VERIFIED HERE

The following checks were executed locally and passed in this environment:

- `.gitlab-ci.yml` parses
- `security/sbom/gitlab-ci-sbom.yml` parses
- `charts/multi-tenant/values-enterprise.yaml` parses
- `charts/x0tta6bl4-commercial/values-enterprise.yaml` parses
- CO-RE generation path is wired (`make -n`)
- Kernel is `>= 6.1`
- BTF is present at `/sys/kernel/btf/vmlinux`
- Coordination docs do not drift back to manual status-board mode
- `compliance/soc2/playbook.md` contains SEV-1 definition
- `compliance/soc2/evidence-matrix.md` exists
- `docs/verification/v1.1-hardening-status.md` exists
- `docs/verification/operator-live-validation-checklist.md` exists
- `ebpf/prod/benchmark-harness.sh` plan-only path exits successfully
- 5G QoS Monitor Bridge integrated and verified via `39 PASS` in `edge/5g/`
- Local Open5GS signaling adapter evidence ingested as VERIFIED HERE:
  `edge/5g/open5gs_live_test.go` signaling mode PASSED and wrote `OPEN5GS_ADAPTER_SESSION_LATEST.json`
  (evidence: `docs/verification/open5gs-live-signaling-20260402T092937Z/`).
- Detached SPIRE PQ attestation validation path completed through `make spire-pq-attestation-validation`; this proves standard leaf SVID issuance plus a separate PQ attestation artifact, not PQ OIDs inside the leaf
- **[2026-04-01]** `e2e-validation.sh` smoke test PASSED 4/4 checks (health, PQC KEM, eBPF metrics, DAO proposals)
- **[2026-04-01]** Local API health check on `MAAS_LIGHT_MODE=true` port 8003 PASSED
- **[2026-04-01]** PQC key encapsulation operation in GhostVPNMetrics PASSED
- **[2026-04-01]** Prometheus metrics endpoint `/metrics` on port 9090 VERIFIED (eBPF ghost_vpn_ebpf counter present)
- **[2026-04-01]** SPIRE/DAO mock routes (`/v1/certs/status`, `/v1/dao/*`, `/dao/proposals`) all PASSED
- **[2026-04-01]** FastAPI `src/core/app.py` middleware stack fixed (removed non-middleware GhostVPNMetrics from `add_middleware`)
- **[2026-04-01]** eBPF benchmark harness executed, results in `ebpf/prod/results/benchmark-20260331T135612Z.json` (measured 30 PPS, target 5M PPS, pass=false)
- **[2026-04-02]** eBPF benchmark harness (LIVE BENCHMARK) executed, results in `ebpf/prod/results/benchmark-20260402T083155Z.json` (measured_pps=138621, target 5M PPS, pass=false). Measurement uses sysfs `tx_packets`/`rx_packets` deltas; XDP re-attach already-attached is treated as recoverable.
- **[2026-04-02]** Live XDP attach on real NIC `enp8s0` PASSED (verify_exit_code=0) evidence in `docs/verification/xdp-live-attach-20260402T081417Z/`
- **[2026-04-02]** Open5GS HTTP bridge harness ran:
  - verification-only mock evidence (mode=`mock-bridge`, verification_only=true). Evidence in `docs/verification/open5gs-http-bridge-evidence-20260402T095825Z/` (`OPEN5GS_HTTP_BRIDGE_LATEST.json`).
  - local real-http evidence (mode=`real-http`, verification_only=false, PASS). Evidence in `docs/verification/open5gs-http-bridge-20260402T104311Z/` (`OPEN5GS_HTTP_BRIDGE_LATEST.json`); bridge responded over `POST /bridge/sessions` and preserved `bridge.log`; claim boundary: transport-proof only (does not upgrade remote production-traffic claims).
- **[2026-04-02]** SBOM generate + CVE gate (Grype on CycloneDX/SPDX) **PASSED** in docker mode: `bash security/sbom/run-local-sbom-check.sh full --tool-mode docker` (exit 0). Mitigations: explicit proxy clearing + `GRYPE_CHECK_FOR_APP_UPDATE=false` in `security/sbom/run-local-sbom-check.sh`; dependency bumps (`agent` gRPC, `requirements.txt` Authlib/black/nltk/pyasn1/PyJWT). Prior proxy/Grype hang notes: `docs/verification/sbom-grype-blocker-20260402T112341Z.txt`.
- **[2026-04-15]** `check_release_consistency.py` **VERIFIED** — script exists and passes; previously reported missing in error.
- **[2026-04-15]** eBPF build artifacts **REMOVED** — deleted 4 committed `.o` files from `src/network/ebpf/` and added to `.gitignore`.
- **[2026-04-15]** Ghost Access security **FIXED** — added rate limiting and xray health checks to `/json` and `/sub` commands (previously bypassed protections).
- **[2026-04-15]** Unit test isolation **FIXED** — added `ensure_subscription_devices` mock to `test_subscription_link_generation` to resolve code/test drift.

---

## VERIFIED VIA SCRIPT/CI

- `ebpf/prod/build-in-docker.sh` provides the containerized build path for the
  nested eBPF module when the local Go toolchain is older than the module
  requirement.
- `scripts/generate_validation_bundle.py` automated Markdown report generation.
- Historical CI run `22822503867` on `release/rc1` produced keyless cosign/Rekor artifacts on 2026-03-08; retain as historical context, not automatic current-gate truth.
- `go test ./edge/5g/... -v` remains the reproducible focused 5G package test path; do not treat prose-only mentions of passing runs as fresh local evidence.
- `.gitlab-ci.yml` contains a draft keyless cosign/Rekor path, but this does not count as current Rekor-backed verification without a CI run and log index artifact.
- `docker-compose.open5gs.yml` is a draft integration helper, not proof of live Open5GS bridge validation.
- `e2e-validation.sh` is a local smoke helper and must exit non-zero on missing dependencies or failed checks before its output can support any stronger claim.

---

## NOT VERIFIED YET

- Helm lint in the reproducible sandboxed local path; use
  `charts/render-in-docker.sh`
- Fresh local `go test ./edge/5g/... -v` output from the current execution context
- `go build ./ebpf/prod` with the current local Go toolchain
- Python unit tests in the `--fast` verification profile (full suite)
- Docker-backed checks when Docker is unreachable in the current execution
  context
- PPS benchmark `>= 5M` (Current `138621 PPS` from latest fresh benchmark is functional proof only; pass=false; Horizon-2 hardware track)
- Live Open5GS HTTP bridge / reproducible remote transport validation beyond local docker bridge endpoints
- Remote Open5GS bridge probe bundles exist in `docs/verification/open5gs-remote-probe-20260402T110306Z/` and `docs/verification/open5gs-remote-probe-20260402T112430Z/`.
  The newer SOCKS-backed run adds `18014` and preserves stderr files, showing a narrower remote failure mode:
  `89.125.1.107:{18014,18080,18083,8010,3000} /bridge/sessions -> curl-exit-52 (Empty reply from server)`,
  `https://maas.01164.com/bridge/sessions -> curl-exit-35`,
  `http://maas.01164.com/bridge/sessions -> HTTP/1.1 409 Conflict`.
  A fresher machine-local probe on 2026-04-12 is preserved in `docs/verification/open5gs-remote-probe-20260412T200635Z/`.
  That endpoint now answers `GET /health` with `{"status":"ok","service":"open5gs-http-bridge-stub"}` and
  `POST /bridge/sessions` returns a `stub-*` session id with fixed `latency_ms=42`, while curl still times out or resets after receiving the body.
  This proves reachability, but it does not qualify as remote production-like bridge evidence.
  A newer probe on 2026-04-13 is preserved in `docs/verification/open5gs-remote-probe-20260413T072848Z/`.
  The remote service now exposes the real bridge handler instead of the stub:
  `GET /health -> {"ok":true,"path":"/bridge/sessions","amf_endpoint":"127.0.0.1:38412","upf_endpoint":"127.0.0.1:8805","bridge":"open5gs-local-http"}`
  and external `POST /bridge/sessions` returns `HTTP/1.0 502 Bad Gateway` with a machine-readable failure body showing
  `SCTP transport failure to AMF (127.0.0.1:38412): connection refused`.
  This is stronger and operationally honest fail-closed bridge evidence, but it is still not a successful production-like session proof.
  A later probe on 2026-04-13 is preserved in `docs/verification/open5gs-remote-probe-20260413T074948Z/`.
  After enabling Docker on the VPS, starting containerized Open5GS AMF/UPF, and pointing the bridge at `89.125.1.107:{38412,8805}`,
  external `POST /bridge/sessions` now returns `HTTP/1.0 200 OK` with `{"accepted": true, "latency_ms": 25, "cause": ""}`.
  This closes the remote bridge success requirement for the bridge path itself.
  A follow-up verification on 2026-04-13 is preserved in `docs/verification/open5gs-remote-probe-20260413T081650Z/`.
  After updating the Open5GS containers to `restart=always`, the VPS survives `systemctl restart docker` without regressing the bridge:
  both `open5gs-remote-amf` and `open5gs-remote-upf` return to `Up`, and external `POST /bridge/sessions`
  still returns `HTTP/1.0 200 OK` with `{"accepted": true, "latency_ms": 25, "cause": ""}`.
  Claim boundary remains: this is remote successful bridge evidence on a reachable VPS-hosted containerized backend, not proof of production traffic or a broader live mobile-core deployment.
- Open5GS integration via `docker-compose.open5gs.yml`
- Live SX1303 HAL binding
- Real DP backend validation
- Playwright E2E against the full production stack beyond the isolated local MaaS validation bundle
- k6 load test against the full production stack beyond the isolated local MaaS validation bundle
- End-to-end Tor/mesh/IPFS/DAO validation against a real stack
- Current keyless cosign + Rekor transparency-log upload from CI identity
- PQ OIDs embedded in the SPIRE leaf SVID
- ML-KEM-enabled SPIRE/TLS handshake

---

## Integrity Flags

- **SEV-1 SECURITY ALERT (MITIGATED)**: `pip-audit` vulnerabilities (CVE-2026-*) have been addressed in `requirements.txt`. System-wide update is pending next Docker build.
- The historical `8.8M PPS` claim is **PURGED** and replaced by empirical physical NIC evidence (49 RX / 142k TX PPS on `enp8s0`).
- 5G↔eBPF Bridge is **VERIFIED** for logic and local metrics. Historical VPS notes from 2026-03-08 remain useful context, but live Open5GS claims must still be backed by fresh reproducible artifacts.
- SPIRE/PQC identity is **VERIFIED** as `standard leaf SVID + detached PQ attestation artifact`. It is **NOT VERIFIED** as PQ OIDs inside the leaf SVID.
