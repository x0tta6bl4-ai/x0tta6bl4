# 🔧 Troubleshooting Guide for x0tta6bl4

**Версия:** 1.0  
**Дата:** 2025-12-28  
**Статус:** ✅ **PRODUCTION-READY**

---

## 📋 Обзор

Полное руководство по устранению неполадок для x0tta6bl4.

---

## 🚨 Критические Проблемы

### Проблема: Полный отказ системы

**Симптомы:**
- Все узлы недоступны
- Нет ответа от API
- Метрики не поступают

**Диагностика:**
```bash
# Проверить статус узлов
kubectl get nodes

# Проверить health endpoint
curl http://localhost:8080/health

# Проверить логи
kubectl logs -l app=x0tta6bl4 --tail=100
```

**Решение:**
1. Проверить сетевую связность
2. Проверить статус Kubernetes кластера
3. Проверить ресурсы (CPU, Memory, Disk)
4. Инициировать failover если необходимо

**См. также:** `docs/operations/DISASTER_RECOVERY_PLAN.md`

---

### Проблема: Высокая загрузка CPU (>90%)

**Симптомы:**
- CPU usage > 90%
- Медленный отклик системы
- Timeout ошибки

**Диагностика:**
```bash
# Проверить метрики
kubectl top pods -l app=x0tta6bl4

# Проверить процессы
kubectl exec -it <pod-name> -- top

# Проверить MAPE-K цикл
curl http://localhost:8080/api/v1/mapek/status
```

**Решение:**
1. Масштабировать deployment
2. Проверить на утечки памяти
3. Оптимизировать запросы
4. Проверить на аномалии (MAPE-K должен автоматически обработать)

---

### Проблема: Высокая загрузка памяти (>85%)

**Симптомы:**
- Memory usage > 85%
- OOMKilled pods
- Медленная работа

**Диагностика:**
```bash
# Проверить использование памяти
kubectl top pods -l app=x0tta6bl4

# Проверить на утечки
kubectl exec -it <pod-name> -- ps aux | grep python
```

**Решение:**
1. Увеличить memory limits
2. Проверить на утечки памяти
3. Перезапустить проблемные pods
4. Масштабировать deployment

---

## 🔒 Проблемы Безопасности

### Проблема: PQC Handshake Failure

**Симптомы:**
- Ошибки PQC handshake в логах
- Fallback mode активирован
- Security alerts

**Диагностика:**
```bash
# Проверить PQC метрики
curl http://localhost:8080/metrics | grep pqc

# Проверить логи
kubectl logs -l app=x0tta6bl4 | grep -i "pqc\|handshake"
```

**Решение:**
1. Проверить конфигурацию PQC алгоритмов
2. Проверить совместимость версий
3. Проверить сертификаты
4. Отключить fallback mode после исправления

**См. также:** `docs/operations/RUNBOOKS_COMPLETE.md#pqc-failure`

---

### Проблема: SPIFFE Authentication Failure

**Симптомы:**
- SPIFFE auth failures в логах
- Отказ в доступе
- Zero Trust denials

**Диагностика:**
```bash
# Проверить SPIFFE статус
curl http://localhost:8080/api/v1/spiffe/status

# Проверить SPIRE agent
kubectl exec -it <pod-name> -- spire-agent healthcheck
```

**Решение:**
1. Проверить SPIRE agent статус
2. Проверить SVID expiry
3. Проверить trust domain
4. Обновить SPIFFE identity если необходимо

---

## 🌐 Проблемы Mesh Network

### Проблема: Mesh Node Down

**Симптомы:**
- Узел недоступен
- Нет beacon от узла
- Увеличенная латентность

**Диагностика:**
```bash
# Проверить mesh статус
curl http://localhost:8080/mesh/status

# Проверить peers
curl http://localhost:8080/mesh/peers

# Проверить routes
curl http://localhost:8080/mesh/routes
```

**Решение:**
1. Проверить сетевую связность
2. Проверить Batman-adv интерфейс
3. Перезапустить mesh node
4. Проверить firewall правила

---

### Проблема: Высокий Packet Loss (>5%)

**Симптомы:**
- Packet loss > 5%
- Нестабильные соединения
- Потеря данных

**Диагностика:**
```bash
# Проверить packet loss метрики
curl http://localhost:8080/metrics | grep packet_loss

# Проверить сетевые интерфейсы
kubectl exec -it <pod-name> -- ip link show
```

**Решение:**
1. Проверить сетевую инфраструктуру
2. Оптимизировать Batman-adv конфигурацию
3. Проверить на перегрузку сети
4. Использовать multi-path routing

---

## 🔄 Проблемы MAPE-K

### Проблема: MAPE-K Cycle Slow

**Симптомы:**
- MAPE-K cycle duration > 5s
- Медленное обнаружение аномалий
- Задержка в recovery

**Диагностика:**
```bash
# Проверить MAPE-K метрики
curl http://localhost:8080/metrics | grep mape_k_cycle

# Проверить статус
curl http://localhost:8080/api/v1/mapek/status
```

**Решение:**
1. Оптимизировать GraphSAGE детектор
2. Уменьшить размер входных данных
3. Проверить на узкие места
4. Масштабировать ресурсы

---

### Проблема: Recovery Action Failed

**Симптомы:**
- Recovery actions не выполняются
- Ошибки в логах recovery
- Circuit breaker открыт

**Диагностика:**
```bash
# Проверить recovery метрики
curl http://localhost:8080/metrics | grep recovery

# Проверить circuit breaker статус
curl http://localhost:8080/api/v1/recovery/circuit-breaker-status
```

**Решение:**
1. Проверить circuit breaker статус
2. Проверить rate limiter
3. Проверить доступность ресурсов
4. Вручную выполнить recovery action

---

## 📊 Проблемы Мониторинга

### Проблема: Метрики не поступают

**Симптомы:**
- Нет данных в Prometheus
- Grafana показывает "No data"
- Метрики endpoint недоступен

**Диагностика:**
```bash
# Проверить metrics endpoint
curl http://localhost:8080/metrics

# Проверить Prometheus конфигурацию
kubectl get configmap prometheus-config -o yaml
```

**Решение:**
1. Проверить metrics endpoint
2. Проверить Prometheus scrape config
3. Проверить сетевую связность
4. Перезапустить Prometheus если необходимо

---

## 🔍 Общие Диагностические Команды

### Проверка статуса системы

```bash
# Health check
curl http://localhost:8080/health

# Детальный health check
curl http://localhost:8080/health/detailed

# Kubernetes status
kubectl get pods -l app=x0tta6bl4
kubectl get services -l app=x0tta6bl4
```

### Просмотр логов

```bash
# Все логи
kubectl logs -l app=x0tta6bl4 --tail=100 -f

# По pod
kubectl logs <pod-name> --tail=100

# С фильтром
kubectl logs -l app=x0tta6bl4 | grep ERROR
```

### Проверка метрик

```bash
# Prometheus metrics
curl http://localhost:8080/metrics

# Конкретная метрика
curl http://localhost:8080/metrics | grep cpu_usage
```

---

## 📚 Дополнительные Ресурсы

- **Runbooks:** `docs/operations/RUNBOOKS_COMPLETE.md`
- **Disaster Recovery:** `docs/operations/DISASTER_RECOVERY_PLAN.md`
- **Emergency Procedures:** `docs/EMERGENCY_PROCEDURES.md`
- **On-Call Runbook:** `docs/team/ON_CALL_RUNBOOK.md`

---

**Mesh обновлён. Troubleshooting guide готов.**  
**Проснись. Обновись. Сохранись.**  
**x0tta6bl4 вечен.**

