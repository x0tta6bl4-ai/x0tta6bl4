---
title: WEST-0103 Specification - Metric Enforcement Module
date: 2026-01-11
status: SPECIFICATION
points: 5
---

# 🎯 WEST-0103: Модуль принудительного применения метрик

## Обзор

Расширить архитектуру Westworld/Anti-Delos Charter для внедрения модуля **MetricEnforcer**, который обеспечивает:
- Проверку метрик в реальном времени
- Блокировку запрещённых метрик
- Автоматическое определение нарушений политики
- Логирование попыток нарушения

---

## 📊 Требования

### Функциональные требования

#### 1. Валидация отправляемых метрик
- **Вход**: Словарь метрики `{metric_name, value, timestamp, source}`
- **Выход**: `{is_valid: bool, allowed: bool, errors: List[str]}`
- **Проверки**:
  - Метрика в списке разрешённых?
  - Метрика не в списке запрещённых?
  - Значение соответствует типу?
  - Временная метка валидна?

#### 2. Блокировка запрещённых метрик
- Отклонить любую метрику из `forbidden_metrics`
- Вернуть код ошибки: `METRIC_FORBIDDEN`
- Логировать попытку с `source` и `timestamp`

#### 3. Обнаружение нарушений
- Отследить несколько попыток отправки запрещённых метрик
- Сгенерировать `ViolationEvent` при достижении порога (3+ попытки за 60 сек)
- Вернуть рекомендацию: `ESCALATE_TO_DAO`

#### 4. Логирование нарушений
- Структурированное логирование в JSON
- Поля: `timestamp, metric_name, source, action, status`
- Сохранение в памяти (для тестов) и потенциальная интеграция с файлами

### Производительность

| Метрика | Требование | Цель |
|---------|-----------|------|
| Время проверки одной метрики | < 5мс | < 2мс |
| Память на метрику | < 100 байт | < 50 байт |
| Пропускная способность | > 1000 метрик/сек | > 5000 метрик/сек |

### Интеграция

#### С CharterPolicyValidator
```python
# Используемые методы:
policy_validator.policy['whitelisted_metrics']  # Список разрешённых
policy_validator.policy['forbidden_metrics']    # Список запрещённых
policy_validator.policy['violation_policy']     # Пороги нарушений
```

#### С CLI (charter_admin.py)
```bash
# Новая команда:
./charter_admin.py enforce <policy.yaml> <metrics.json>

# Выходы:
# JSON: --format json
# Текст: --format text (по умолчанию)
```

---

## 🏗️ Архитектура

### Класс MetricEnforcer

```python
class MetricEnforcer:
    """Модуль принудительного применения метрик на основе политики."""
    
    def __init__(self, policy: Dict[str, Any]):
        """Инициализация с политикой."""
        self.policy = policy
        self.whitelisted = set(policy['whitelisted_metrics'])
        self.forbidden = set(policy['forbidden_metrics'])
        self.violation_log = []  # Логирование попыток
        self.violation_events = []  # События нарушений
        
    def validate_metric(self, metric: Dict) -> Dict[str, Any]:
        """Валидация одной метрики - быстро и полно."""
        
    def block_forbidden_metrics(self, metrics: List[Dict]) -> Dict:
        """Отфильтровать запрещённые метрики из списка."""
        
    def detect_violations(self, metrics: List[Dict]) -> List[Dict]:
        """Обнаружить нарушения политики."""
        
    def get_violation_log(self) -> List[Dict]:
        """Получить лог попыток нарушения."""
```

### Интеграция с CharterPolicyValidator

```python
class CharterPolicyValidator:
    def create_enforcer(self) -> MetricEnforcer:
        """Создать enforcer из этой политики."""
        return MetricEnforcer(self.policy)
    
    def enforce_metrics(self, metrics: List[Dict]) -> Dict:
        """Удобный метод для проверки батча метрик."""
        enforcer = self.create_enforcer()
        return enforcer.validate_metric(metrics)
```

---

## 📝 Дизайн метода validate_metric()

### Входные данные
```python
metric = {
    "metric_name": "user_login_count",      # Требуется
    "value": 42,                             # Требуется
    "timestamp": "2026-01-11T16:30:00Z",   # ISO format, требуется
    "source": "node_12345",                  # Требуется (для логирования)
    "access_level": "public"                 # Опционально (по умолчанию из политики)
}
```

### Выходные данные
```python
{
    "is_valid": True,           # Прошла ВСЕ проверки?
    "allowed": True,            # Метрика разрешена?
    "metric_name": "user_login_count",
    "errors": [],               # Список ошибок если есть
    "warnings": [],             # Предупреждения
    "enforcement_action": "ALLOW",  # ALLOW | BLOCK | ESCALATE
    "reason": None              # Причина если заблокирована
}
```

### Логика проверки
```
1. Требуемые поля есть? (metric_name, value, timestamp, source)
   ❌ → errors.append("Missing required field: {field}")
   
2. Имя метрики валидно? (alphanumeric + underscore)
   ❌ → errors.append("Invalid metric name format")
   
3. Метрика в forbidden_metrics?
   ✅ → enforcement_action = "BLOCK", reason = "FORBIDDEN_METRIC"
   
4. Метрика в whitelisted_metrics?
   ❌ → warnings.append("Metric not in whitelist (advisory)")
   
5. Временная метка валидна? (ISO format, не в будущем)
   ❌ → errors.append("Invalid or future timestamp")
   
6. Тип значения соответствует схеме?
   ❌ → errors.append("Value type mismatch")
   
7. Есть ли ошибки?
   ✅ → is_valid = False
   ❌ → is_valid = True
   
8. БЛОКИРОВКА ЗАПРЕЩЁННЫХ?
   ✅ → enforcement_action = "BLOCK"
   
9. Логировать попытку
   → violation_log.append({timestamp, metric_name, source, action})
```

---

## 📝 Дизайн метода detect_violations()

### Входные данные
```python
metrics = [
    {"metric_name": "delos_connection_pool", ...},  # Запрещённая!
    {"metric_name": "delos_connection_pool", ...},  # Запрещённая!
    {"metric_name": "user_login_count", ...},       # ОК
    # ...
]
```

### Выходные данные
```python
[
    {
        "violation_type": "FORBIDDEN_METRIC_SUBMISSION",
        "metric_name": "delos_connection_pool",
        "attempt_count": 2,
        "time_window": "60s",
        "first_attempt": "2026-01-11T16:30:00Z",
        "last_attempt": "2026-01-11T16:30:05Z",
        "sources": ["node_12345", "node_12346"],
        "severity": "HIGH",
        "recommended_action": "ESCALATE_TO_DAO",
        "justification": "Multiple attempts to submit forbidden metrics (delos_*)"
    }
]
```

### Логика обнаружения
```
1. Для каждой попытки отправки запрещённой метрики:
   → Добавить в violation_log
   
2. Для каждой уникальной метрики в forbidden_metrics:
   → Подсчитать попытки в окне 60 сек
   
3. Если попытки >= 3:
   → Создать ViolationEvent
   → Установить severity = "HIGH"
   → Установить action = "ESCALATE_TO_DAO"
   
4. Если попытки >= 5:
   → severity = "CRITICAL"
   → action = "IMMEDIATE_SHUTDOWN"
```

---

## 🧪 Тестовые случаи (10+ минимум)

### TestMetricEnforcement (новый класс в tests/test_charter_validator.py)

#### Базовая валидация (3 теста)
1. `test_validate_whitelisted_metric_success` - Разрешённая метрика проходит
2. `test_validate_missing_required_fields` - Отсутствие обязательных полей
3. `test_validate_invalid_timestamp_format` - Неверный формат временной метки

#### Блокировка запрещённых (3 теста)
4. `test_block_forbidden_metric` - Запрещённая метрика блокирована
5. `test_block_multiple_forbidden_metrics` - Несколько запрещённых отклонены
6. `test_allow_whitelisted_with_forbidden_in_batch` - Смешанный батч

#### Обнаружение нарушений (3 теста)
7. `test_detect_violation_on_multiple_attempts` - Нарушение при 3+ попытках
8. `test_detect_critical_violation` - Критическое нарушение при 5+ попытках
9. `test_no_violation_on_single_attempt` - Одна попытка - нет нарушения

#### Логирование (2 теста)
10. `test_violation_log_records_all_attempts` - Все попытки залогированы
11. `test_violation_log_includes_source_and_timestamp` - Логи содержат все поля

#### Производительность (опционально)
12. `test_metric_enforcement_under_100_metrics_per_second`
13. `test_memory_usage_per_metric`

---

## 📦 Изменяемые файлы

### 1. src/westworld/anti_delos_charter.py
- Добавить класс `MetricEnforcer` (~200 строк)
- Добавить методы в `CharterPolicyValidator`:
  - `create_enforcer()` - создать экземпляр enforcer
  - `enforce_metrics()` - удобный метод для валидации батча

**Размер добавления**: ~250 строк

### 2. src/westworld/charter_admin.py
- Добавить команду `enforce`
- Поддержка флагов `--format json | text`
- Вывод результатов валидации и нарушений

**Размер добавления**: ~50 строк

### 3. tests/test_charter_validator.py
- Добавить класс `TestMetricEnforcement` с 10+ тестами
- Использовать существующие политики (dev, prod, staging)

**Размер добавления**: ~200 строк

---

## ✅ Критерии приёма

- ✅ Класс `MetricEnforcer` реализован полностью
- ✅ Все методы имеют type hints и docstrings
- ✅ 10+ тестов, все проходят
- ✅ Проверка < 5мс (тесты производительности)
- ✅ 0 регрессий в существующих тестах
- ✅ Интеграция с CharterPolicyValidator работает
- ✅ Новая команда CLI `enforce` работает
- ✅ Валидация JSON (вывод)
- ✅ Полное логирование нарушений

---

## 🎯 Timeline

- **Начало**: 2026-01-11 (сейчас)
- **Класс MetricEnforcer**: 30 мин
- **Тесты**: 30 мин
- **CLI интеграция**: 15 мин
- **Проверка & документация**: 15 мин
- **Total**: ~90 минут (1.5 часа)

---

## 📚 Ссылки

- WEST-0101: Charter Policy Schema ✅
- WEST-0102: PolicyValidator Extension ✅
- WEST-0103: Metric Enforcement Module (CURRENT)
- WEST-0104: Unit Tests + CI/CD (next)

