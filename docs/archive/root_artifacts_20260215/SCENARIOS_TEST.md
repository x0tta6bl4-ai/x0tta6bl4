# 🎯 x0tta6bl4 — End-to-End Сценарии Тестирования

**Философия**: Система готова, когда работает для пользователя, а не когда код написан.

---

## Сценарий 1: Mesh из 10 узлов запускается и маршрутизирует

### Описание
Запустить 10 Docker-контейнеров, каждый — узел mesh-сети. Проверить, что трафик маршрутизируется между любыми двумя узлами.

### Предусловия
- Docker и Docker Compose установлены
- Образ `x0tta6bl4:latest` собран

### Шаги

```bash
# 1. Запуск 10 узлов
docker-compose -f docker-compose.mesh-test.yml up -d --scale node=10

# 2. Проверка что все узлы видят друг друга
for i in $(seq 1 10); do
  docker exec node-$i curl -s http://localhost:8080/mesh/peers | jq '.count'
done
# Ожидание: каждый узел видит >= 3 peers

# 3. Тест маршрутизации: отправить пакет от node-1 к node-10
docker exec node-1 curl -X POST http://localhost:8080/mesh/route \
  -d '{"destination": "node-10", "payload": "test"}'
# Ожидание: {"status": "delivered", "hops": N, "latency_ms": X}

# 4. Проверить метрики
docker exec node-1 curl http://localhost:8080/metrics | grep mesh_
```

### Метрики успеха

| Метрика | Цель | Критично? |
|---------|------|-----------|
| Все 10 узлов online | 100% | ✅ Да |
| Каждый узел видит >= 3 peers | 100% | ✅ Да |
| Пакет доставлен от 1 к 10 | Да | ✅ Да |
| Latency < 100ms | < 100ms | ⚠️ Желательно |
| Количество hops | <= 4 | ⚠️ Желательно |

### Текущий статус
- [ ] Тест создан
- [ ] Тест проходит
- [ ] Время прохождения: ___

---

## Сценарий 2: Один узел падает → система переконфигурируется

### Описание
После того как mesh из 10 узлов работает, убить один промежуточный узел. Проверить, что трафик автоматически перенаправляется через другие маршруты.

### Предусловия
- Сценарий 1 пройден
- Mesh из 10 узлов запущен

### Шаги

```bash
# 1. Определить маршрут от node-1 к node-10
ROUTE=$(docker exec node-1 curl -s http://localhost:8080/mesh/route/node-10)
echo "Текущий маршрут: $ROUTE"
# Пример: node-1 → node-5 → node-7 → node-10

# 2. Убить промежуточный узел (например, node-5)
docker stop node-5
START_TIME=$(date +%s%3N)

# 3. Подождать и проверить новый маршрут
sleep 5
NEW_ROUTE=$(docker exec node-1 curl -s http://localhost:8080/mesh/route/node-10)
END_TIME=$(date +%s%3N)

# 4. Вычислить MTTR
MTTR=$((END_TIME - START_TIME))
echo "MTTR: ${MTTR}ms"

# 5. Проверить что маршрут изменился
if [ "$ROUTE" != "$NEW_ROUTE" ]; then
  echo "✅ Маршрут переконфигурирован"
else
  echo "❌ Маршрут НЕ изменился"
fi

# 6. Тест доставки по новому маршруту
docker exec node-1 curl -X POST http://localhost:8080/mesh/route \
  -d '{"destination": "node-10", "payload": "after-failure"}'
```

### Метрики успеха

| Метрика | Цель | Критично? |
|---------|------|-----------|
| Маршрут изменился | Да | ✅ Да |
| MTTR (время восстановления) | < 5000ms | ✅ Да |
| Пакет доставлен после сбоя | Да | ✅ Да |
| k-disjoint path использован | Да | ⚠️ Желательно |

### Текущий статус
- [ ] Тест создан
- [ ] Тест проходит
- [ ] MTTR измерен: ___ ms

---

## Сценарий 3: Пользователь подключается через Telegram бот

### Описание
Пользователь взаимодействует с ботом: получает trial, скачивает конфиг, подключается к VPN.

### Предусловия
- Telegram бот запущен с валидным токеном
- VPN сервер (Xray) запущен и доступен
- База данных инициализирована

### Шаги (ручные через Telegram)

```
Пользователь → Бот:
1. /start
   Ожидание: Приветствие + меню

2. /trial
   Ожидание: "Trial активирован! Доступен до: DD.MM.YYYY"

3. /config
   Ожидание: 
   - QR код (если доступен)
   - VLESS ссылка
   - Файл .conf

4. Импорт конфига в v2rayNG/v2rayN
   Ожидание: Конфиг импортируется без ошибок

5. Подключение к VPN
   Ожидание: Соединение установлено

6. Проверка IP
   curl https://ipinfo.io/ip
   Ожидание: IP = 89.125.1.107 (или другой сервер)

7. /status
   Ожидание: "Активная подписка. Осталось X дней."
```

### Метрики успеха

| Метрика | Цель | Критично? |
|---------|------|-----------|
| /start отвечает | < 1s | ✅ Да |
| /trial создаёт пользователя | Да | ✅ Да |
| /config возвращает валидный конфиг | Да | ✅ Да |
| VPN соединение устанавливается | Да | ✅ Да |
| IP меняется на сервер | Да | ✅ Да |
| Ping через VPN | < 100ms | ⚠️ Желательно |

### Текущий статус
- [ ] Бот запущен
- [ ] Trial работает
- [ ] Конфиг генерируется
- [ ] VPN подключается
- [ ] IP проверен

---

## Сценарий 4: MAPE-K цикл обнаруживает и исправляет проблему

### Описание
Искусственно создать аномалию (высокий CPU или packet loss), проверить что MAPE-K цикл:
1. Обнаруживает проблему (Monitor)
2. Анализирует причину (Analyze)
3. Создаёт план восстановления (Plan)
4. Исполняет действия (Execute)
5. Записывает в базу знаний (Knowledge)

### Предусловия
- FastAPI app запущен
- MAPE-K loop активен
- Prometheus метрики доступны

### Шаги

```bash
# 1. Проверить что MAPE-K loop работает
curl http://localhost:8080/health
# Ожидание: {"status": "ok", "version": "3.0.0"}

# 2. Проверить метрики сознания
curl http://localhost:8080/metrics | grep consciousness_
# Ожидание: consciousness_state = 3 (HARMONIC)

# 3. Искусственно нагрузить CPU
docker exec app stress --cpu 4 --timeout 30s &

# 4. Подождать один цикл MAPE-K (60s по умолчанию, или 10s в MYSTICAL)
sleep 15

# 5. Проверить что состояние изменилось
curl http://localhost:8080/metrics | grep consciousness_
# Ожидание: consciousness_state = 1 или 2 (MYSTICAL или CONTEMPLATIVE)

# 6. Проверить логи на наличие healing действий
docker logs app | grep -i "healing\|recovery\|aggressive"
# Ожидание: "Aggressive healing initiated" или подобное

# 7. После снятия нагрузки — проверить восстановление
sleep 60
curl http://localhost:8080/metrics | grep consciousness_
# Ожидание: consciousness_state = 3 (HARMONIC) — восстановлен
```

### Метрики успеха

| Метрика | Цель | Критично? |
|---------|------|-----------|
| Аномалия обнаружена | < 30s | ✅ Да |
| Состояние изменилось на MYSTICAL | Да | ✅ Да |
| Healing действие запущено | Да | ✅ Да |
| Система восстановилась | Да | ✅ Да |
| Время восстановления | < 120s | ⚠️ Желательно |

### Текущий статус
- [ ] Тест создан
- [ ] Monitor обнаруживает аномалию
- [ ] Analyze определяет причину
- [ ] Plan создаёт стратегию
- [ ] Execute исполняет
- [ ] Knowledge записывает

---

## Сценарий 5: Post-Quantum Handshake между узлами

### Описание
Два узла устанавливают защищённый канал с использованием PQ криптографии.

### ⚠️ КРИТИЧЕСКОЕ ЗАМЕЧАНИЕ
**Текущая реализация = MOCK.** Этот сценарий покажет, что "криптография" сейчас — это хэши, а не реальный NTRU/Kyber.

### Шаги

```bash
# 1. Запустить два узла
docker-compose up node-1 node-2 -d

# 2. Инициировать handshake от node-1 к node-2
curl -X POST http://localhost:8080/security/handshake \
  -H "Content-Type: application/json" \
  -d '{"node_id": "node-1", "algorithm": "hybrid"}'
# Ожидание: {"status": "handshake_initiated", "algorithm": "NTRU+ECDSA"}

# 3. Проверить что shared secret создан
# (В текущей реализации это hashlib, не реальный KEM)

# 4. Отправить зашифрованное сообщение
curl -X POST http://localhost:8080/security/encrypt \
  -d '{"peer_id": "node-2", "message": "test"}'
# Ожидание: {"ciphertext": "..."}

# 5. Расшифровать на node-2
curl -X POST http://node-2:8080/security/decrypt \
  -d '{"peer_id": "node-1", "ciphertext": "..."}'
# Ожидание: {"plaintext": "test"}
```

### Метрики успеха

| Метрика | Цель | Текущее состояние |
|---------|------|-------------------|
| Handshake завершён | Да | ⚠️ MOCK |
| Реальный NTRU/Kyber | Да | ❌ НЕТ |
| Shared secret уникален | Да | ⚠️ Основан на хэшах |
| Latency handshake | < 50ms | ⚠️ Не измерено |

### Текущий статус
- [x] Структура создана
- [ ] Реальный liboqs интегрирован
- [ ] Бенчмарк latency
- [ ] Security аудит

---

## 📊 Сводная Таблица Сценариев

| # | Сценарий | Статус | Критичность |
|---|----------|--------|-------------|
| 1 | Mesh 10 узлов | ❓ Не проверен | 🔴 Critical |
| 2 | Failover при падении узла | ❓ Не проверен | 🔴 Critical |
| 3 | Telegram bot flow | ⚠️ Частично работает | 🔴 Critical |
| 4 | MAPE-K healing | ❓ Не проверен | 🟡 High |
| 5 | PQ Handshake | ❌ MOCK | 🔴 Critical (для безопасности) |

---

## 🎯 Как использовать этот документ

1. **Проходи сценарии по порядку**
2. **Отмечай статус** ([ ] → [x])
3. **Записывай реальные метрики**
4. **Система готова, когда все 🔴 Critical сценарии проходят**

---

*Документ создан: 24 декабря 2025*
*Подход: End-to-End, не проценты*


