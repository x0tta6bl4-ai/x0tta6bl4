# 🔧 Установка ML зависимостей для Continuity Ledger

**Версия:** Phase 1 (RAG Integration)

> ⚠️ **ВАЖНО:** Если предыдущая установка зависла систему, используйте **безопасную установку**:
> ```bash
> bash scripts/install_ledger_ml_deps_safe.sh
> ```
> См. [Безопасная установка](./LEDGER_ML_DEPS_SAFE_INSTALL.md) для деталей.  
**Дата:** 2026-01-03

---

## 📋 Обзор

Для полной функциональности semantic search в Continuity Ledger Phase 1 необходимы следующие ML зависимости:

- **hnswlib** — HNSW-based векторный индекс для быстрого поиска
- **sentence-transformers** — для генерации embeddings и re-ranking
- **numpy** — для работы с векторами (обычно уже установлен)

---

## 🚀 Быстрая установка

### Вариант 1: Через скрипт (рекомендуется)

```bash
bash scripts/install_ledger_ml_deps.sh
```

Скрипт автоматически:
- Проверит наличие Python и pip
- Установит все зависимости
- Проверит установку
- Протестирует импорты

### Вариант 2: Через requirements файл

```bash
pip install -r requirements-ledger-ml.txt
```

### Вариант 3: Через pyproject.toml (если настроен)

```bash
pip install -e ".[ml]"
```

### Вариант 4: Прямая установка

```bash
pip install hnswlib sentence-transformers
```

---

## ✅ Проверка установки

### Автоматическая проверка

```bash
python3 scripts/check_ledger_ml_deps.py
```

Скрипт проверит:
- Наличие всех зависимостей
- Работоспособность компонентов
- Интеграцию с проектом
- Готовность Phase 1

### Ручная проверка

```python
# Проверка hnswlib
python3 -c "import hnswlib; print('✅ hnswlib OK')"

# Проверка sentence-transformers
python3 -c "from sentence_transformers import SentenceTransformer; print('✅ sentence-transformers OK')"

# Проверка интеграции
python3 -c "
import sys
sys.path.insert(0, 'src')
from storage.vector_index import HNSW_AVAILABLE, SENTENCE_TRANSFORMERS_AVAILABLE
print(f'HNSW: {HNSW_AVAILABLE}')
print(f'SentenceTransformers: {SENTENCE_TRANSFORMERS_AVAILABLE}')
"
```

---

## 📦 Детали зависимостей

### hnswlib

**Версия:** >=0.7.0, <1.0.0  
**Назначение:** HNSW-based векторный индекс для быстрого semantic search

**Установка:**
```bash
pip install hnswlib
```

**Проверка:**
```python
import hnswlib
index = hnswlib.Index(space='cosine', dim=384)
print("✅ hnswlib работает")
```

### sentence-transformers

**Версия:** >=2.2.0, <6.0.0  
**Назначение:** 
- Генерация embeddings для документов
- Re-ranking результатов поиска через CrossEncoder

**Установка:**
```bash
pip install sentence-transformers
```

**Автоматически устанавливает:**
- torch (если не установлен)
- transformers
- другие зависимости

**Проверка:**
```python
from sentence_transformers import SentenceTransformer, CrossEncoder
model = SentenceTransformer('all-MiniLM-L6-v2')
print("✅ sentence-transformers работает")
```

---

## 🔍 Устранение проблем

### Проблема: hnswlib не устанавливается

**Возможные причины:**
- Отсутствует компилятор C++
- Проблемы с pip

**Решение:**
```bash
# Ubuntu/Debian
sudo apt-get install build-essential

# macOS
xcode-select --install

# Затем повторите установку
pip install hnswlib
```

### Проблема: sentence-transformers медленно устанавливается

**Причина:** Загружает большие модели при первом использовании

**Решение:** Это нормально, модели кешируются после первой загрузки

### Проблема: Конфликты версий

**Решение:**
```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements-ledger-ml.txt
```

---

## 🧪 Тестирование после установки

### 1. Базовые тесты

```bash
pytest tests/ledger/test_rag_search_basic.py -v
```

### 2. Полные тесты (требуют зависимости)

```bash
pytest tests/ledger/test_rag_search.py -v
```

### 3. Интеграционные тесты

```bash
pytest tests/integration/test_ledger_api.py -v
```

### 4. Функциональное тестирование

```bash
# Индексирование
python scripts/index_ledger_in_rag.py

# Поиск
python scripts/ledger_rag_query.py "Какие метрики?"

# Интерактивный поиск
python scripts/ledger_search_interactive.py
```

---

## 📊 Статус зависимостей

После установки проверьте статус:

```bash
python3 scripts/check_ledger_ml_deps.py
```

Ожидаемый вывод:
```
✅ Все критические зависимости установлены и работают
✅ Phase 1 готов к использованию с полным semantic search
```

---

## 🔄 Обновление зависимостей

### Обновление всех зависимостей

```bash
pip install --upgrade -r requirements-ledger-ml.txt
```

### Обновление конкретной зависимости

```bash
pip install --upgrade hnswlib
pip install --upgrade sentence-transformers
```

---

## 📝 Примечания

1. **Размер установки:**
   - hnswlib: ~5-10 MB
   - sentence-transformers: ~500 MB (включая модели)

2. **Время установки:**
   - hnswlib: 1-2 минуты
   - sentence-transformers: 5-10 минут (зависит от интернета)

3. **Дисковое пространство:**
   - Модели кешируются в `~/.cache/torch/sentence_transformers/`
   - Размер кеша: ~500 MB

4. **Производительность:**
   - Первый запуск медленнее (загрузка моделей)
   - Последующие запуски быстрые (модели в памяти)

---

## 🎯 Следующие шаги

После установки зависимостей:

1. ✅ Проверьте установку: `python3 scripts/check_ledger_ml_deps.py`
2. ✅ Запустите тесты: `pytest tests/ledger/test_rag_search.py -v`
3. ✅ Индексируйте ledger: `python scripts/index_ledger_in_rag.py`
4. ✅ Протестируйте поиск: `python scripts/ledger_rag_query.py "Какие метрики?"`

---

**Последнее обновление:** 2026-01-03

