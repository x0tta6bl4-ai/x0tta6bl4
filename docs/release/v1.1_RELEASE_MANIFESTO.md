# Release Manifesto: x0tta6bl4 v1.1 "Empirical Integrity"

**Today, we transition from an ambitious vision to a verified engineering baseline.**

## 1. The Pivot to Honesty
We have deliberately purged the simulated performance claim of 8.8M PPS. While mathematically possible in controlled lab environments, it did not reflect our current physical hardware reality. 

**Our new baseline is 142,000 TX PPS.** It is lower, but it is **real**. It was measured on physical Intel hardware, verified by the agent swarm, and is publicly auditable. We choose integrity over vanity.

## 2. Production-Ready Infrastructure
Release v1.1 GA is not just a collection of scripts; it is a monitored, secured, and validated mesh network stack.
- **Live Monitoring:** Full Prometheus/Grafana stack integrated with eBPF kernel telemetry.
- **5G Interoperability:** NGAP signaling verified against live Open5GS core on production nodes.
- **Supply Chain Trust:** Keyless signing via Sigstore/Rekor ensures that every byte you run is exactly what we built.
- **Multi-Tenant Security:** Zero-lateral-movement enforced at the kernel level via XDP and Cilium.

## 3. The Path to Horizon-2 (1M+ PPS)
v1.1 is our stable floor. From here, we begin the high-performance campaign. Our research into `XDP_REDIRECT` and `AF_XDP` Zero-Copy is already underway. We have identified the hardware path (Intel X710) required to reach the next million.

## 4. Availability
The **v1.1 GA** release is now available on the `main` branch. 
It is stable, honest, and ready for your most critical workloads.

---
**Verified by the Swarm.**  
**2026-03-08**
