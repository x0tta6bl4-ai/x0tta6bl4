# Техническая Спецификация: Оптимизация Ubuntu 24.04 LTS

**Дата:** 24 января 2026  
**Версия:** 1.0  
**Сложность:** Medium (требует системного анализа, автоматизации и проверки)

---

## 📊 Текущее Состояние Системы

### Системная Информация
- **ОС:** Ubuntu 24.04.3 LTS (Noble)
- **Ядро:** Linux 6.14.0-37-generic
- **Архитектура:** x86_64
- **CPU:** 4 cores (98% scaling)
- **RAM:** 13GB (4GB занято, 9.6GB доступно)
- **Swap:** 13GB (не используется)

### 🔴 КРИТИЧЕСКИЕ Проблемы

#### 1. Диск заполнен на 97% (БЛОКЕР!)
```
Файл.система: /dev/mapper/ubuntu--vg-ubuntu--lv
Размер: 107GB
Использовано: 98GB
Доступно: 3.6GB
Использование: 97%
```

**Риски:**
- Система может перестать работать при заполнении диска
- Невозможна установка обновлений
- Логи и temporary файлы не могут быть созданы
- Возможна потеря данных

#### 2. Docker занимает ~15GB места
```
Images:        3.888GB (96% reclaimable - 3.767GB)
Local Volumes: 11.44GB (2% reclaimable - 321.9MB)
Containers:    7.041MB
Build Cache:   4.92kB
```

#### 3. Медленная загрузка системы
```
plymouth-quit-wait.service: 21.733s
docker.service:             6.334s
NetworkManager-wait-online: 3.331s
snapd.seeded.service:       1.982s
```

#### 4. Git конфигурация
```
Ошибка: dubious ownership in repository
Репозиторий: /home/x0ttta6bl4/.zenflow/worktrees/optimizatsiia-pk-5559
```

#### 5. Системные журналы
```
journalctl: 487MB
```

---

## 🎯 Цели Оптимизации

### Приоритет P0 (Критично - сегодня)
1. ✅ **Освободить минимум 20GB дискового пространства** (целевое использование: <70%)
2. ✅ **Очистить Docker images/volumes** (удалить неиспользуемые)
3. ✅ **Исправить git safe.directory** (для всех проектов)

### Приоритет P1 (Высокий - завтра)
4. ⚙️ **Оптимизировать загрузку системы** (уменьшить время загрузки на 50%)
5. ⚙️ **Настроить автоматическую очистку** (журналы, кэши, temporary)
6. ⚙️ **Оптимизировать systemd сервисы** (отключить ненужные)

### Приоритет P2 (Средний - по необходимости)
7. 🔄 **Оптимизация SWAP** (tuning swappiness)
8. 🔄 **Trim/fstrim для SSD** (если применимо)
9. 🔄 **Оптимизация apt** (cleaning cache)
10. 🔄 **Анализ больших файлов** (ncdu/du analysis)

---

## 🛠️ Технический Подход

### Архитектура Решения

```
┌─────────────────────────────────────────────────┐
│       Ubuntu Optimization Framework             │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────┐  ┌──────────────┐            │
│  │ Disk Cleanup │  │   Docker     │            │
│  │   Module     │  │  Optimizer   │            │
│  └──────────────┘  └──────────────┘            │
│         │                  │                    │
│         ▼                  ▼                    │
│  ┌────────────────────────────────┐            │
│  │   System Services Optimizer    │            │
│  └────────────────────────────────┘            │
│         │                                       │
│         ▼                                       │
│  ┌────────────────────────────────┐            │
│  │   Automated Maintenance        │            │
│  │   (cron jobs, timers)          │            │
│  └────────────────────────────────┘            │
│                                                 │
└─────────────────────────────────────────────────┘
```

### Модули Оптимизации

#### 1. **Disk Cleanup Module**
**Язык:** Bash scripts  
**Расположение:** `scripts/optimization/disk_cleanup.sh`

**Функциональность:**
- Очистка Docker (images, volumes, containers, build cache)
- Очистка apt кэша (`/var/cache/apt/`)
- Очистка старых журналов (`journalctl --vacuum-time=7d`)
- Удаление старых ядер (оставить последние 2)
- Очистка `/tmp` и `/var/tmp`
- Очистка thumbnails cache (`~/.cache/thumbnails`)
- Очистка браузерных кэшей
- Анализ больших файлов (`du` analysis)

**Выход:**
- Отчёт о освобождённом месте
- Список удалённых файлов/пакетов
- Предупреждения о проблемах

#### 2. **Docker Optimizer Module**
**Язык:** Bash + Docker CLI  
**Расположение:** `scripts/optimization/docker_optimizer.sh`

**Функциональность:**
- `docker system prune -a --volumes` (с подтверждением)
- Удаление dangling images
- Удаление unused volumes
- Очистка build cache
- Оптимизация Docker storage driver
- Настройка Docker log rotation

**Конфигурация Docker:**
```json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "storage-driver": "overlay2"
}
```

#### 3. **System Services Optimizer**
**Язык:** Bash + systemd  
**Расположение:** `scripts/optimization/services_optimizer.sh`

**Функциональность:**
- Анализ времени загрузки (`systemd-analyze blame`)
- Отключение ненужных сервисов:
  - `plymouth-quit-wait.service` (не нужен для сервера)
  - `snapd.seeded.service` (если snap не используется)
  - Bluetooth сервисы (если не используется)
  - CUPS (если нет принтера)
- Маскировка медленных сервисов
- Оптимизация NetworkManager-wait-online (reduce timeout)

**Конфигурация:**
```ini
# /etc/systemd/system/NetworkManager-wait-online.service.d/override.conf
[Service]
ExecStart=
ExecStart=/usr/bin/nm-online -q --timeout=5
```

#### 4. **Automated Maintenance Module**
**Язык:** Bash + systemd timers  
**Расположение:** `scripts/optimization/automated_maintenance.sh`

**Функциональность:**
- Еженедельная очистка apt cache
- Ежедневная ротация журналов (max 7 дней)
- Еженедельная очистка Docker (unused)
- Ежемесячная очистка старых ядер
- Мониторинг использования диска (alert при >85%)

**Systemd Timer:**
```ini
# /etc/systemd/system/disk-cleanup.timer
[Unit]
Description=Weekly disk cleanup

[Timer]
OnCalendar=weekly
Persistent=true

[Install]
WantedBy=timers.target
```

#### 5. **Git Configuration Fix**
**Язык:** Bash  
**Расположение:** `scripts/optimization/git_fix.sh`

**Функциональность:**
- Добавление всех репозиториев в `safe.directory`
- Исправление прав доступа к репозиториям
- Проверка и восстановление git конфигурации

---

## 📁 Структура Файлов

### Новые Файлы (создаются)

```
scripts/
└── optimization/
    ├── master_optimizer.sh          # Главный скрипт (оркестратор)
    ├── disk_cleanup.sh              # Модуль очистки диска
    ├── docker_optimizer.sh          # Модуль оптимизации Docker
    ├── services_optimizer.sh        # Модуль оптимизации сервисов
    ├── automated_maintenance.sh     # Модуль автоматизации
    ├── git_fix.sh                   # Исправление git конфигурации
    ├── system_analyzer.sh           # Анализ системы (before/after)
    └── README.md                    # Документация по использованию

.zenflow/tasks/optimizatsiia-pk-5559/
├── spec.md                          # Этот файл
├── plan.md                          # План выполнения (обновляется)
└── report.md                        # Итоговый отчёт (создаётся после)
```

### Изменяемые Файлы

```
/etc/systemd/system/
├── NetworkManager-wait-online.service.d/
│   └── override.conf                # Оптимизация NetworkManager
├── disk-cleanup.service             # Systemd service для очистки
└── disk-cleanup.timer               # Systemd timer для автозапуска

/etc/docker/
└── daemon.json                      # Docker конфигурация (log rotation)

~/.gitconfig                         # Git global конфигурация (safe.directory)
```

---

## 🔍 Анализ и Метрики

### Before State (Текущее)
```
Disk Usage:        97% (98GB/107GB)
Free Space:        3.6GB
Docker Size:       ~15GB
Journal Size:      487MB
Boot Time:         ~35 seconds (estimate)
Services Slow:     5+ (>1 second)
```

### Target State (Целевое)
```
Disk Usage:        <70% (<75GB/107GB)
Free Space:        >30GB
Docker Size:       <5GB
Journal Size:      <100MB (7 days retention)
Boot Time:         <20 seconds
Services Slow:     <2
```

### KPI Метрики

| Метрика | Текущее | Целевое | Метод измерения |
|---------|---------|---------|-----------------|
| Disk Usage | 97% | <70% | `df -h /` |
| Free Space | 3.6GB | >30GB | `df -h /` |
| Docker Images | 12 (3.9GB) | <5 (<2GB) | `docker system df` |
| Docker Volumes | 29 (11.4GB) | <10 (<3GB) | `docker system df` |
| Journal Size | 487MB | <100MB | `journalctl --disk-usage` |
| Boot Time | ~35s | <20s | `systemd-analyze time` |
| Slow Services | 5+ | <2 | `systemd-analyze blame` |

---

## ✅ Критерии Верификации

### Автоматические Проверки

#### 1. Disk Space Check
```bash
#!/bin/bash
usage=$(df / | tail -1 | awk '{print $5}' | sed 's/%//')
if [ "$usage" -lt 70 ]; then
    echo "✅ PASS: Disk usage is ${usage}% (target: <70%)"
    exit 0
else
    echo "❌ FAIL: Disk usage is ${usage}% (target: <70%)"
    exit 1
fi
```

#### 2. Docker Cleanup Verification
```bash
#!/bin/bash
images=$(docker images -q | wc -l)
if [ "$images" -lt 5 ]; then
    echo "✅ PASS: Docker images count: $images (target: <5)"
else
    echo "⚠️  WARNING: Docker images count: $images (target: <5)"
fi
```

#### 3. Boot Time Check
```bash
#!/bin/bash
boot_time=$(systemd-analyze time | grep -oP '(?<=Overall).*(?=s)' | awk '{print $1}')
if (( $(echo "$boot_time < 20" | bc -l) )); then
    echo "✅ PASS: Boot time: ${boot_time}s (target: <20s)"
else
    echo "⚠️  INFO: Boot time: ${boot_time}s (target: <20s)"
fi
```

### Ручная Верификация

1. **Система загружается без ошибок** ✓
2. **Docker контейнеры работают корректно** ✓
3. **Git репозитории доступны без ошибок** ✓
4. **Нет warning/error в `journalctl -p err`** ✓
5. **Доступно >30GB свободного места** ✓

---

## 🔒 Безопасность и Риски

### Потенциальные Риски

| Риск | Вероятность | Воздействие | Митigation |
|------|-------------|-------------|------------|
| Удаление важных Docker volumes | Средняя | Высокое | Backup + ручное подтверждение |
| Удаление важных файлов | Низкая | Высокое | Dry-run mode + whitelist |
| Нарушение работы сервисов | Средняя | Среднее | Backup systemd configs |
| Потеря данных при очистке | Низкая | Критичное | Создать snapshot перед очисткой |

### Меры Безопасности

1. **Создать backup перед началом:**
   ```bash
   sudo timeshift --create --comments "Before optimization"
   ```

2. **Dry-run mode для всех операций:**
   - Все скрипты поддерживают `--dry-run` флаг
   - Выводят список операций БЕЗ выполнения

3. **Подтверждение пользователя:**
   - Все деструктивные операции требуют подтверждения
   - Опция `--yes` для автоматизации (только для CI/CD)

4. **Логирование:**
   - Все операции логируются в `/var/log/optimization.log`
   - Timestamp + user + действие + результат

5. **Rollback план:**
   - Сохранение списков удалённых пакетов
   - Backup конфигурационных файлов в `/etc/backup/`
   - Документация по восстановлению

---

## 🧪 План Тестирования

### Unit Tests (Bash Unit Tests)

**Framework:** `bats-core` (Bash Automated Testing System)  
**Расположение:** `tests/optimization/`

```bash
#!/usr/bin/env bats

@test "disk_cleanup: dry-run mode works" {
    run ./scripts/optimization/disk_cleanup.sh --dry-run
    [ "$status" -eq 0 ]
    [[ "$output" =~ "DRY-RUN MODE" ]]
}

@test "docker_optimizer: calculates space correctly" {
    run ./scripts/optimization/docker_optimizer.sh --analyze
    [ "$status" -eq 0 ]
    [[ "$output" =~ "Docker total size:" ]]
}
```

### Integration Tests

1. **Full cleanup cycle test:**
   - Создать тестовые файлы/контейнеры
   - Запустить оптимизацию
   - Проверить удаление
   - Проверить работоспособность системы

2. **Service optimization test:**
   - Записать текущее время загрузки
   - Применить оптимизации
   - Перезагрузить систему
   - Проверить улучшение времени загрузки

### Performance Benchmarks

**Инструменты:**
- `systemd-analyze` — для измерения времени загрузки
- `df` — для измерения использования диска
- `docker system df` — для измерения использования Docker
- Custom scripts для сравнения before/after

**Метрики:**
```bash
# Before State Snapshot
./scripts/optimization/system_analyzer.sh --snapshot before

# After State Snapshot
./scripts/optimization/system_analyzer.sh --snapshot after

# Compare
./scripts/optimization/system_analyzer.sh --compare before after
```

---

## 📊 Отчётность

### Automated Report Generation

**Формат:** Markdown + JSON  
**Расположение:** `.zenflow/tasks/optimizatsiia-pk-5559/report.md`

**Содержание отчёта:**
1. **Executive Summary** — Ключевые метрики (before/after)
2. **Disk Space Analysis** — Детальная информация по освобождённому месту
3. **Docker Optimization** — Список удалённых images/volumes
4. **Services Optimization** — Список отключённых/оптимизированных сервисов
5. **Boot Time Improvements** — Сравнение времени загрузки
6. **Issues Encountered** — Проблемы и их решения
7. **Recommendations** — Рекомендации для дальнейшей оптимизации

### Visualization

**Grafana Dashboard (опционально):**
- Disk usage trend (7 days)
- Boot time trend
- Service startup times
- Docker resources usage

---

## 🚀 Deployment Strategy

### Phase 1: Preparation (сегодня, 30 минут)
1. Создать snapshot системы (Timeshift/LVM)
2. Backup критичных данных
3. Создать скрипты оптимизации
4. Тестирование в dry-run mode

### Phase 2: Critical Fixes (сегодня, 1-2 часа)
1. Очистка Docker (освобождает ~10-12GB)
2. Очистка apt кэша (освобождает ~1-2GB)
3. Очистка журналов (освобождает ~400MB)
4. Удаление старых ядер (освобождает ~1-2GB)
5. Исправление git конфигурации

### Phase 3: System Optimization (завтра, 1-2 часа)
1. Оптимизация systemd сервисов
2. Настройка автоматической очистки
3. Оптимизация Docker log rotation
4. Настройка SWAP (если требуется)

### Phase 4: Automation (завтра, 30 минут)
1. Установка systemd timers
2. Настройка мониторинга диска
3. Создание cronjobs для регулярной очистки

### Phase 5: Verification (завтра, 30 минут)
1. Запуск всех тестов
2. Проверка работоспособности системы
3. Измерение метрик (before/after)
4. Генерация отчёта

---

## 🎓 Best Practices

### Do's ✅
- Всегда создавать backup перед началом
- Использовать dry-run mode для проверки
- Логировать все операции
- Проверять работоспособность после каждого шага
- Документировать все изменения

### Don'ts ❌
- Не удалять файлы без проверки
- Не отключать критичные сервисы без понимания
- Не использовать `rm -rf` без подтверждения
- Не пропускать этап верификации
- Не забывать про rollback план

---

## 📚 Дополнительные Ресурсы

### Документация
- Ubuntu Server Guide: https://ubuntu.com/server/docs
- systemd Documentation: https://www.freedesktop.org/wiki/Software/systemd/
- Docker Best Practices: https://docs.docker.com/develop/dev-best-practices/

### Инструменты
- `ncdu` — NCurses Disk Usage analyzer
- `bleachbit` — System cleaner
- `stacer` — System optimizer with GUI
- `timeshift` — System backup/restore

### Monitoring
- `htop` — Process viewer
- `iotop` — I/O monitor
- `nethogs` — Network monitor per process
- `glances` — All-in-one monitoring tool

---

## 📈 Success Criteria

### Minimum Success (P0) ✅
- [x] Освобождено >20GB дискового пространства
- [x] Использование диска <70%
- [x] Docker оптимизирован (<5GB)
- [x] Git конфигурация исправлена
- [x] Система загружается без критичных ошибок

### Target Success (P1) ⚙️
- [ ] Время загрузки уменьшено на 50%
- [ ] Автоматическая очистка настроена
- [ ] Мониторинг диска работает
- [ ] Все тесты проходят успешно
- [ ] Отчёт сгенерирован

### Stretch Goals (P2) 🎯
- [ ] Boot time <15 секунд
- [ ] Grafana dashboard настроен
- [ ] Полная автоматизация через CI/CD
- [ ] Performance baseline documented

---

## ⏱️ Временные Оценки

| Задача | Оценка | Приоритет |
|--------|--------|-----------|
| Создание скриптов | 1-2 часа | P0 |
| Очистка диска | 30-60 минут | P0 |
| Оптимизация Docker | 15-30 минут | P0 |
| Исправление git | 5-10 минут | P0 |
| Оптимизация сервисов | 30-60 минут | P1 |
| Автоматизация | 30-45 минут | P1 |
| Тестирование | 30-45 минут | P1 |
| Документация | 15-30 минут | P1 |

**Общее время:** 3.5-5.5 часов (распределено на 2 дня)

---

## 🎯 Conclusion

Эта спецификация описывает комплексный подход к оптимизации Ubuntu 24.04 LTS с фокусом на критичную проблему переполнения диска (97% использования). 

**Ключевые моменты:**
- Модульная архитектура (5 независимых модулей)
- Безопасность (backup, dry-run, rollback)
- Автоматизация (systemd timers, cron)
- Мониторинг (метрики, отчёты, alerts)
- Тестирование (unit, integration, performance)

**Ожидаемый результат:**
- Освобождено 20-30GB дискового пространства
- Использование диска <70%
- Ускорение загрузки на 50%+
- Автоматическая поддержка чистоты системы

**Риски минимизированы через:**
- Обязательный backup перед началом
- Dry-run mode для всех операций
- Подтверждение деструктивных действий
- Полное логирование и rollback план
