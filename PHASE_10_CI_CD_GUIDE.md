🚀 ФАЗА 10: CI/CD - ГАЙД ПО ПЕРВОМУ RELEASE

════════════════════════════════════════════════════════════════════════════

ТЕКУЩЕЕ СОСТОЯНИЕ
════════════════════════════════════════════════════════════════════════════

✅ Версия обновлена: 3.1.0 → 3.2.0
✅ GitHub Actions workflows созданы (3 файла):
   • .github/workflows/test.yml (Testing & Code Quality)
   • .github/workflows/docker.yml (Docker Build & Push)
   • .github/workflows/release.yml (Releases)
✅ Semantic versioning настроен (.bumpversion.cfg)
✅ Release notes generator создан (scripts/generate_release_notes.py)
✅ CI/CD документация написана


ЧТО СОЗДАНО
════════════════════════════════════════════════════════════════════════════

WORKFLOWS:

1. test.yml
   • Запускается на: push to main/develop, PRs
   • Тестирует на: Python 3.10, 3.11, 3.12
   • Проверяет: тесты, линтинг, типы, формат
   • Результат: 67/67 tests ✅

2. docker.yml
   • Запускается на: push, tags, PRs
   • Реестр: GHCR (GitHub Container Registry)
   • Образы: development, production
   • Метаданные: branch tags, version tags, SHA

3. release.yml
   • Запускается на: git tag v*.*.*
   • Проверяет версию и запускает тесты
   • Генерирует release notes
   • Публикует на PyPI
   • Создает GitHub Release

КОНФИГУРАЦИЯ:

• .bumpversion.cfg - Автоматическое версионирование
• CI_CD_DOCUMENTATION.md - Полная документация

СКРИПТЫ:

• scripts/generate_release_notes.py - Генерация release notes
• ci-cd-simulate.sh - Локальное тестирование CI/CD


════════════════════════════════════════════════════════════════════════════

ПОДГОТОВКА К ПЕРВОМУ RELEASE (v3.2.0)
════════════════════════════════════════════════════════════════════════════

ШАГИ:

1. ИНИЦИАЛИЗИРОВАТЬ ГИТ (если еще нет)
   ────────────────────────────────────
   $ cd /mnt/AC74CC2974CBF3DC
   
   # Если репозиторий уже существует
   $ git status
   
   # Если НЕ существует
   $ git init
   $ git add .
   $ git commit -m "chore: initial commit v3.2.0"


2. ПРОВЕРИТЬ ВСЕ ТЕСТЫ ЛОКАЛЬНО
   ─────────────────────────────
   $ bash ci-cd-simulate.sh
   
   Должно быть:
   ✅ pytest: 67/67 passed
   ✅ Version check passed
   ✅ All phases passed


3. УБЕДИТЬСЯ WORKFLOWS ГОТОВЫ
   ──────────────────────────
   $ ls -la .github/workflows/
   
   Должны быть:
   ✅ test.yml
   ✅ docker.yml
   ✅ release.yml


4. СОЗДАТЬ ПЕРВЫЙ TAG
   ──────────────────
   
   ВАРИАНТ A: Используя bumpversion
   $ pip install bump2version
   $ bump2version minor  # 3.1.0 → 3.2.0
   $ git push origin main --tags
   
   ВАРИАНТ B: Вручную
   $ git tag -a v3.2.0 -m "Release 3.2.0: Full CI/CD automation"
   $ git push origin main --tags


════════════════════════════════════════════════════════════════════════════

ТРЕБУЕМЫЕ GITHUB SECRETS
════════════════════════════════════════════════════════════════════════════

ВАЖНО! Если публиковать на PyPI, нужны secrets:

1. ПЕРЕЙТИ В GITHUB SETTINGS
   ──────────────────────────
   Repo → Settings → Secrets and variables → Actions

2. СОЗДАТЬ SECRETS
   ────────────────
   
   PYPI_API_TOKEN (обязателен для публикации)
   • Создать на https://pypi.org/manage/account/tokens/
   • Scope: Entire account
   • Скопировать token в GitHub secret
   
   CODECOV_TOKEN (опционально)
   • Создать на https://codecov.io/
   • Только для приватных репо


═══════════════════════════════════════════════════════════════════════════

ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ ПЕРЕД РЕЛИЗОМ
═══════════════════════════════════════════════════════════════════════════

Убедитесь всё работает:

# 1. Запустить тесты
$ python3 -m pytest tests/test_mape_k.py -v
✅ Должно: 67 passed

# 2. Проверить качество кода
$ bash ci-cd-simulate.sh
✅ Все фазы passed

# 3. Проверить версию
$ python3 -c "from src.mape_k import __version__; print(__version__)"
✅ Должно: 3.2.0

# 4. Проверить package
$ python3 -m build
✅ Должны быть dist/*.tar.gz и dist/*.whl


════════════════════════════════════════════════════════════════════════════

СОЗДАНИЕ И ПУБЛИКАЦИЯ RELEASE
════════════════════════════════════════════════════════════════════════════

ПОЛНЫЙ ПРОЦЕСС:

1. Убедитесь, что на главной ветке latest код:
   $ git checkout main
   $ git pull

2. Создайте tag:
   $ git tag -a v3.2.0 -m "Release 3.2.0: Full CI/CD automation with GitHub Actions"

3. Push tag на GitHub:
   $ git push origin v3.2.0
   
   (или если используете bumpversion:)
   $ bump2version minor && git push origin main --tags

4. GitHub Actions автоматически:
   ✅ Проверяет версию (tag должна совпадать с pyproject.toml)
   ✅ Запускает тесты (67/67 must pass)
   ✅ Генерирует release notes
   ✅ Публикует на PyPI (если PYPI_API_TOKEN настроена)
   ✅ Создает GitHub Release
   ✅ Пушит Docker image


МОНИТОРИТЬ ПРОЦЕСС:

# GitHub CLI
$ gh run list
$ gh run view <run-id> --log

# Веб-интерфейс
https://github.com/x0tta6bl4/x0tta6bl4/actions


════════════════════════════════════════════════════════════════════════════

ПРОВЕРКА УСПЕШНОГО RELEASE
════════════════════════════════════════════════════════════════════════════

После того как CI/CD завершится:

1. Проверить GitHub Release
   https://github.com/x0tta6bl4/x0tta6bl4/releases

2. Проверить Docker image (если создается)
   $ docker pull ghcr.io/x0tta6bl4/x0tta6bl4:v3.2.0

3. Проверить PyPI (если публикуется)
   $ pip install --upgrade x0tta6bl4==3.2.0
   $ python3 -c "import x0tta6bl4; print(x0tta6bl4.__version__)"


════════════════════════════════════════════════════════════════════════════

ЧАСТО ВСТРЕЧАЮЩИЕСЯ ПРОБЛЕМЫ
════════════════════════════════════════════════════════════════════════════

Проблема: Release не запускается
Решение:
  • Tag должна быть в формате v*.*.*
  • Tag должна быть на GitHub (git push --tags)
  • Проверить Actions tab для ошибок

Проблема: Тесты падают на CI
Решение:
  • Запустить локально: pytest tests/ -v
  • Проверить версию Python (3.10+)
  • Проверить зависимости: pip install -e ".[dev,testing]"

Проблема: PyPI публикация падает
Решение:
  • PYPI_API_TOKEN не настроена? Пропустить публикацию
  • Token истек? Создать новый
  • Версия уже существует? Bump version

Проблема: Docker build падает
Решение:
  • Dockerfile exists?
  • Docker installed на runner?
  • Dockerfile syntax правильный?


════════════════════════════════════════════════════════════════════════════

СЛЕДУЮЩИЕ ШАГИ ПОСЛЕ v3.2.0
════════════════════════════════════════════════════════════════════════════

Следующий release:

1. Создать feature branch
   $ git checkout -b feat/next-feature develop

2. Сделать изменения, закоммитить
   $ git add .
   $ git commit -m "feat: add new feature"

3. Push и create PR
   $ git push origin feat/next-feature
   $ gh pr create --base develop

4. После merge в develop, merge в main
   $ git checkout main
   $ git merge develop
   $ git push

5. Bump version и release
   $ bump2version minor
   $ git push origin main --tags


════════════════════════════════════════════════════════════════════════════

ИТОГО: ЧТО ПОЛУЧИЛИ В ФАЗЕ 10
════════════════════════════════════════════════════════════════════════════

✅ Полностью автоматизированный CI/CD pipeline
✅ GitHub Actions workflows для тестирования и релизов
✅ Semantic versioning через bumpversion
✅ Автоматическая генерация release notes
✅ Docker image building и pushing
✅ PyPI publishing (когда нужно)
✅ GitHub Releases creation
✅ Локальная имитация CI/CD (ci-cd-simulate.sh)
✅ Полная документация

СИСТЕМА ГОТОВА К PRODUCTION-GRADE RELEASES! 🚀


════════════════════════════════════════════════════════════════════════════

НАЧНИТЕ RELEASE:

1. $ bash ci-cd-simulate.sh         (Проверить локально)
2. $ git tag -a v3.2.0 -m "..."     (Создать tag)
3. $ git push origin v3.2.0         (Push на GitHub)
4. 🎉 GitHub Actions автоматически делает всё!

════════════════════════════════════════════════════════════════════════════
