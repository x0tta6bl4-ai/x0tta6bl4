# 🔧 Fix: localhost:4040 не работает

**Проблема**: Ngrok не запущен или не работает

---

## ⚡ Быстрое решение (3 шага)

### Шаг 1: Запустить сервер (если не запущен)

```bash
cd /mnt/AC74CC2974CBF3DC/web/demo
python3 -m http.server 8080
```

**Проверить**: Откройте http://localhost:8080/causal-dashboard.html в браузере

---

### Шаг 2: Запустить ngrok (в НОВОМ терминале)

```bash
ngrok http 8080
```

**Важно**: Запустите в отдельном терминале, не закрывайте его!

---

### Шаг 3: Получить URL

**Вариант A**: Ngrok покажет URL в терминале:
```
Forwarding: https://xxxxx.ngrok.io -> http://localhost:8080
```

**Вариант B**: Откройте в браузере: http://localhost:4040

**Вариант C**: Через API:
```bash
curl -s http://localhost:4040/api/tunnels | python3 -c "
import sys, json
data = json.load(sys.stdin)
tunnels = data.get('tunnels', [])
if tunnels:
    print(tunnels[0]['public_url'] + '/causal-dashboard.html')
"
```

---

## 🆘 Если все еще не работает

### Проверка 1: Сервер запущен?
```bash
curl http://localhost:8080/causal-dashboard.html
```
Должен вернуть HTML код.

### Проверка 2: Ngrok запущен?
```bash
ps aux | grep ngrok
```
Должен показать процесс ngrok.

### Проверка 3: Порт 8080 свободен?
```bash
lsof -i :8080
```
Должен показать python процесс.

### Проверка 4: Ngrok authtoken настроен?
```bash
ngrok config check
```

---

## ✅ После получения URL

1. Скопируйте URL: `https://xxxxx.ngrok.io/causal-dashboard.html`
2. Откройте `FINAL_EMAIL_READY.md`
3. Замените `[DEMO_URL_HERE]` на ваш URL
4. Отправьте email!

---

**Попробуйте сейчас: запустите `ngrok http 8080` в новом терминале!** 🚀

