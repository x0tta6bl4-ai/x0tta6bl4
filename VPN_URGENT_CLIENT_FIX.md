# 🔴 СРОЧНОЕ ИСПРАВЛЕНИЕ - Клиент Подключается Через DIRECT

## ❌ Проблема

**Ошибка в логах:**
```
ERROR connection: open connection to 89.125.1.107:39829 using outbound/direct[direct]: 
dial tcp 89.125.1.107:39829: connect: connection refused
```

**Причина:** В v2rayN есть routing rule, которое отправляет трафик к 89.125.1.107 через `direct` вместо `proxy`.

---

## ✅ РЕШЕНИЕ (ВЫПОЛНИТЬ СЕЙЧАС)

### В v2rayN:

1. **Откройте v2rayN**

2. **Настройки → Routing** (или Параметры → Routing)

3. **ПОСМОТРИТЕ ВСЕ ПРАВИЛА** - найдите правило которое отправляет 89.125.1.107 через `direct`

4. **УДАЛИТЕ это правило** полностью

5. **ДОБАВЬТЕ В САМОЕ НАЧАЛО** (первым в списке!) новое правило:

   ```
   Type: field
   IP: 89.125.1.107/32
   Outbound: proxy
   ```

6. **Сохраните**

7. **ПОЛНОСТЬЮ ЗАКРОЙТЕ v2rayN** (не просто перезапуск, а полностью закройте)

8. **Откройте v2rayN заново**

9. **Подключитесь к VPN** (89.125.1.107:39829)

---

## 🔍 Как Найти Проблемное Правило

**В v2rayN Routing найдите правило где:**
- IP содержит: `89.125.1.107` или `89.125.1.0/24` или любой диапазон включающий этот IP
- Outbound: `direct`
- Или правило с `geosite:ru` которое может зацепить сервер

**УДАЛИТЕ его!**

---

## ✅ Правильная Конфигурация Routing

**Порядок правил (сверху вниз):**

1. **VPN сервер** → `proxy` ✅
   ```
   Type: field
   IP: 89.125.1.107/32
   Outbound: proxy
   ```
   **ДОЛЖНО БЫТЬ ПЕРВЫМ!**

2. **Российские домены** → `direct`
   ```
   Type: field
   Domain: sber.ru, gosuslugi.ru, yandex.ru и т.д.
   Outbound: direct
   ```

3. **Остальное** → `proxy` (по умолчанию)

---

## 🧪 Проверка После Исправления

**В логах v2rayN должно быть:**
```
from 127.0.0.1:PORT accepted tcp:89.125.1.107:39829 [socks -> proxy]
```

**НЕ должно быть:**
```
dial tcp 89.125.1.107:39829 using outbound/direct[direct]
```

---

## ⚠️ Важно

1. **Правило для 89.125.1.107 должно быть ПЕРВЫМ** в списке правил
2. **Формат IP:** `89.125.1.107/32` (не просто `89.125.1.107`)
3. **Outbound:** `proxy` (не `Proxy` или другое)
4. **Полностью перезапустите v2rayN** после изменений

---

## 🔧 Альтернативное Решение

**Если не можете найти правило в интерфейсе:**

1. Найдите файл конфигурации v2rayN:
   - Linux: `~/.config/v2rayN/config.json`
   - Windows: `%AppData%\v2rayN\config.json`

2. Откройте файл и найдите секцию `routing`

3. Убедитесь что первое правило:
   ```json
   {
     "type": "field",
     "ip": ["89.125.1.107/32"],
     "outboundTag": "proxy"
   }
   ```

4. Сохраните файл

5. Перезапустите v2rayN

---

## ✅ После Исправления

**Ожидается:**
- ✅ Подключение к 89.125.1.107:39829 через `proxy`
- ✅ VPN работает
- ✅ Нет ошибок connection refused

**Сервер работает, проблема только в routing на клиенте!**

---

## 🚀 Быстрое Решение

**Если ничего не помогает:**

1. В v2rayN: **Настройки → Routing**
2. **УДАЛИТЕ ВСЕ ПРАВИЛА** временно
3. **Добавьте только одно правило:**
   - IP: `89.125.1.107/32` → `proxy`
4. Сохраните и перезапустите
5. Подключитесь к VPN

После того как VPN заработает, можно добавить обратно правила для российских доменов.

---

## ✅ Итог

**Проблема:** Routing на клиенте отправляет 89.125.1.107 через `direct`
**Решение:** Удалить это правило, добавить правило 89.125.1.107 → `proxy` первым в списке
**Сервер:** ✅ Работает

**После исправления routing VPN заработает!** 🚀

