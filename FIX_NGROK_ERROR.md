# 🔧 Исправление ERR_NGROK_3200

**Проблема**: Ngrok показывает "endpoint is offline"

**Причина**: Сервер на localhost:8080 не отвечает или ngrok не может подключиться

---

## ✅ Быстрое исправление (2 команды)

### Шаг 1: Убедиться что сервер работает

```bash
# Проверить
curl http://localhost:8080/causal-dashboard.html

# Если не работает, запустить:
cd /mnt/AC74CC2974CBF3DC/web/demo
python3 -m http.server 8080
```

**Оставьте этот терминал открытым!**

---

### Шаг 2: В НОВОМ терминале запустить ngrok

```bash
ngrok http 8080
```

**Важно**: 
- Сервер должен работать в первом терминале
- Ngrok запускается во втором терминале
- Оба терминала должны быть открыты

---

## 🆘 Если все еще не работает

### Проверка 1: Сервер запущен?

```bash
curl http://localhost:8080/causal-dashboard.html
```

Должен вернуть HTML код страницы.

### Проверка 2: Ngrok подключен?

В выводе ngrok должно быть:
```
Forwarding: https://xxxxx.ngrok.io -> http://localhost:8080
```

### Проверка 3: Firewall блокирует?

```bash
# Проверить что порт открыт
netstat -tlnp | grep 8080
```

---

## 🚀 Правильная последовательность

**Терминал 1:**
```bash
cd /mnt/AC74CC2974CBF3DC/web/demo
python3 -m http.server 8080
# Оставьте открытым!
```

**Терминал 2:**
```bash
ngrok http 8080
# Скопируйте URL отсюда
```

---

## ✅ После исправления

1. Ngrok покажет URL: `https://xxxxx.ngrok.io`
2. Откройте: `https://xxxxx.ngrok.io/causal-dashboard.html`
3. Страница должна загрузиться
4. Скопируйте URL для email

---

**Готово!** Запустите сервер в одном терминале, ngrok в другом 🚀

