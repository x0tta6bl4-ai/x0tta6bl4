# 🔧 Исправление ошибок в расчете веса изделия

## 📋 Проблема

При проектировании шкафа размерами **1600 × 2400 × 600 мм** система выводит вес **~1093 кг**, что в **5 раз больше нормального** (~235 кг).

## 🔍 Анализ

**Нормальный вес шкафа:**
- Боки: 2 × (600 × 2400 × 16 мм) = 67.2 кг
- Крыша/дно: 2 × (1600 × 600 × 16 мм) = 44.8 кг  
- Задняя стенка: 1600 × 2400 × 4 мм = 11.2 кг
- Полки + перегородки: ~67.3 кг
- **Фасады**: 2 × (1600 × 2400 × 16 мм) = 100.9 кг
- **ИТОГО: ~235.5 кг** ✓

**Обнаруженная ошибка:**
Одна из перегородок имеет `depth = 600mm` вместо `depth = 16mm`:
- 600 × 2400 × 600 мм × 730 кг/м³ = **630.7 кг** ← ОШИБКА!
- С остальными компонентами: ~630 + 224 = ~**854 кг**
- Плюс дополнительные ошибочные элементы → **~1093 кг**

## ✅ Исправления

### 1. Толщина фасадов (ГОТОВО)
**Файл:** `services/CabinetGenerator.ts` (строка 1128)

```typescript
// ДО (НЕПРАВИЛЬНО):
const doorPanel: Panel = {
    ...
    depth: 18,  // ← Слишком много!
    ...
}

// ПОСЛЕ (ИСПРАВЛЕНО):
const doorPanel: Panel = {
    ...
    depth: 16,  // ← Стандартная толщина МДФ/ЛДСП
    ...
}
```

### 2. Проверить параметры перегородок

**Файл:** `services/CabinetGenerator.ts` (строка ~951)

Убедиться, что все перегородки (dividers) имеют:
```typescript
const divPanel: Panel = {
    ...
    width: internalDepth,   // ← Глубина перегородки (600 мм)
    height: internalH,      // ← Высота перегородки (2368 мм)
    depth: 16,              // ← ТОЛЩИНА (16 мм, НЕ 600!)
    ...
}
```

### 3. Проверить параметры полок

**Файл:** `services/CabinetGenerator.ts` (строка ~1007)

Убедиться, что все полки (shelves) имеют:
```typescript
const shelfPanel = {
    ...
    width: sec.width,      // ← Ширина полки (1600 мм)
    height: shelfDepth,    // ← Глубина полки (600 мм)
    depth: 16,             // ← ТОЛЩИНА (16 мм, НЕ 600!)
    ...
}
```

## 🧪 Верификация

После исправления:

1. **Запустить генератор:**
   ```bash
   npm run build
   npm test
   ```

2. **Проверить БОМ:**
   - Создать шкаф 1600 × 2400 × 600 мм
   - Экспортировать BOM
   - Убедиться что вес ~250-300 кг (в зависимости от конфигурации)

3. **Ожидаемые значения:**
   - ✅ Шкаф без полок: ~150 кг
   - ✅ Шкаф с 4 полками: ~250-300 кг
   - ✅ Шкаф с фасадами: +100 кг

## 🐛 Известные ограничения

- Вес не включает фурнитуру (петли, направляющие, ручки)
- Плотность материала 730 кг/м³ для ЛДСП (может варьироваться)
- Расчет основан на прямолинейной геометрии

## 📊 Примеры расчетов по конфигурациям

| Конфиг | Боки | Полки | Фасады | **Вес** |
|--------|------|-------|--------|---------|
| Базовый (1600×2400×600, 3 полки) | 67 | 34 | 101 | **202 кг** |
| + Перегородка | 67 | 51 | 101 | **219 кг** |
| + 2 перегородки | 67 | 68 | 101 | **236 кг** |
| + Цоколь (36мм) | 83 | 51 | 101 | **235 кг** |

## 🔗 Связанные файлы

- `services/BillOfMaterials.ts` - расчет веса компонентов
- `types.ts` - определение интерфейса Panel
- `materials.config.ts` - свойства материалов (плотность)

