# АРХИТЕКТУРНЫЕ ДИАГРАММЫ ПРОФЕССИОНАЛЬНЫХ CAD

---

## 1. МНОГОУРОВНЕВАЯ АРХИТЕКТУРА (как Fusion 360)

```
┌────────────────────────────────────────────────────────────────┐
│                     USER INTERFACE LAYER                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  React Components                                        │  │
│  │  ├── Viewport (Three.js 3D view)                        │  │
│  │  ├── Properties Panel (Material, Dimensions)            │  │
│  │  ├── Feature Tree (операции)                            │  │
│  │  ├── Toolbar (Create, Edit, Transform)                  │  │
│  │  └── Toast Notifications                                │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────────────┘
                     │ Zustand state management
┌────────────────────▼─────────────────────────────────────────────┐
│              DATA MANAGEMENT LAYER                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  CAD Document Model                                      │  │
│  │  ├── Feature Tree (Pad, Pocket, Hole, etc)             │  │
│  │  ├── Parameters (width: 600, height: 2000)             │  │
│  │  ├── Constraints (distance, parallel, etc)             │  │
│  │  └── Material & Properties                              │  │
│  │                                                          │  │
│  │  Command History (Undo/Redo)                            │  │
│  │  ├── Command Stack                                      │  │
│  │  ├── Undo Pointer                                       │  │
│  │  └── Max 100 actions in memory                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────────────┘
                     │ Geometry generation
┌────────────────────▼─────────────────────────────────────────────┐
│           GEOMETRY ENGINE LAYER (Web Worker)                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Geometry Calculations (не блокирует UI!)              │  │
│  │  ├── Boolean Operations (Union, Difference)            │  │
│  │  ├── Mesh Generation from parameters                   │  │
│  │  ├── Simplification (LOD levels)                       │  │
│  │  ├── Constraint Solving                                │  │
│  │  └── Bounding Box Calculation                           │  │
│  │                                                          │  │
│  │  OCCT.js (optional)                                     │  │
│  │  └── Advanced CAD kernel operations                     │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────────────┘
                     │ BufferGeometry, Materials
┌────────────────────▼─────────────────────────────────────────────┐
│             RENDERING LAYER (Three.js)                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Scene Management                                        │  │
│  │  ├── Scene Graph                                         │  │
│  │  ├── Camera (Orthographic for CAD)                       │  │
│  │  └── Lights & Environment                                │  │
│  │                                                          │  │
│  │  Performance Optimizations                              │  │
│  │  ├── LOD System (дальние объекты упрощены)             │  │
│  │  ├── Frustum Culling (не рисуем невидимое)             │  │
│  │  ├── InstancedMesh (1000 одинаковых = 1 render)        │  │
│  │  ├── Batch Rendering (группируем by material)          │  │
│  │  └── Normal Matrix Caching                              │  │
│  │                                                          │  │
│  │  Material System                                         │  │
│  │  ├── MeshStandardMaterial (PBR)                         │  │
│  │  ├── Material Caching (避免 duplicates)                 │  │
│  │  └── Texture Management                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────────────┘
                     │ WebGLRenderer
┌────────────────────▼─────────────────────────────────────────────┐
│              GPU / CANVAS                                         │
│  <canvas> element → WebGL context → GPU rendering                │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2. DATA FLOW (как изменения распространяются)

```
User Action (drag, create, transform)
        │
        ▼
┌──────────────────────────────┐
│ Mouse Event Handler           │
│ (Scene3D.tsx)                 │
└──────────────┬────────────────┘
               │
        ┌──────▼──────┐
        │ Raycasting  │
        │ (что выбрали│
        │  мышкой?)   │
        └──────┬──────┘
               │
        ┌──────▼──────────────────┐
        │ Create Command           │
        │ (MovePartCommand, etc)  │
        └──────┬──────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ CommandHistory.execute()       │
        │ (добавить в historized и       │
        │  invalidate undo/redo)         │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ Update CADDocument state       │
        │ (panels, features, params)    │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ invalidateGeometry()           │
        │ (пересчет нужен)               │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ WebWorker.postMessage()        │
        │ (отправить в фоновый thread)  │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ Worker calculates geometry     │
        │ (не блокирует UI!)             │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ Worker sends result back       │
        │ postMessage(geometry)          │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ updateGeometry() in React      │
        │ (обновляем Three.js meshes)    │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ Trigger render()               │
        │ (requestAnimationFrame)        │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ Three.js renders new frame     │
        │ (в GPU)                        │
        └──────┬────────────────────────┘
               │
        ┌──────▼────────────────────────┐
        │ Canvas displays result         │
        │ (пользователь видит изменения) │
        └───────────────────────────────┘
```

---

## 3. FEATURE TREE ЗАВИСИМОСТИ

```
Документ: "Kitchen Cabinet"
│
├── Sketch_Base
│   │   (эскиз основания 600x2000)
│   │   parameters: {width: 600, height: 2000}
│   │
│   └─► используется в:
│
├── Pad_Body (экструзия)
│       params: {depth: 18}
│       dependsOn: [Sketch_Base]
│       geometry: BoxGeometry(600, 2000, 18)
│
│       ├─► используется в:
│       │
│       ├── Hole_Drill_1
│       │   params: {position: {x: 300, y: 1000}, diameter: 10}
│       │   dependsOn: [Pad_Body]
│       │   geometry: Pad_Body - Cylinder(r=5, h=18)
│       │
│       ├── Fillet_Edges
│       │   params: {radius: 2}
│       │   dependsOn: [Pad_Body]
│       │   geometry: Pad_Body with rounded edges
│       │
│       └── Hole_Drill_2
│           params: {position: {x: 300, y: 2000}, diameter: 10}
│           dependsOn: [Pad_Body]
│
├── Assembly_Hardware
│       dependsOn: [Pad_Body, Hole_Drill_1, Hole_Drill_2]
│       components:
│       ├── Screw_1 (1000 штук)
│       │   using InstancedMesh for performance
│       │
│       └── Dowel_1 (500 штук)
│           using InstancedMesh for performance
```

**Ключевой момент:**
- Если изменить `Sketch_Base.width = 700` (было 600)
- → Pad_Body пересчитается
- → Hole_Drill_1, Hole_Drill_2 пересчитаются
- → Assembly_Hardware обновится
- ВСЁ АВТОМАТИЧЕСКИ!

---

## 4. UNDO/REDO MECHANISM (Command History)

```
История команд на разных этапах

НАЧАЛО: История пуста
  stack: []
  pointer: -1

ДЕЙСТВИЕ 1: Создать Pad
  stack: [CreatePadCommand]
  pointer: 0  ◄── текущая команда
  
ДЕЙСТВИЕ 2: Переместить Hole
  stack: [CreatePadCommand, MoveHoleCommand]
  pointer: 1  ◄── текущая команда

ДЕЙСТВИЕ 3: Добавить Fillet
  stack: [CreatePadCommand, MoveHoleCommand, AddFilletCommand]
  pointer: 2  ◄── текущая команда

НАЖАЛИ UNDO:
  stack: [CreatePadCommand, MoveHoleCommand, AddFilletCommand]
  pointer: 1  ◄── MoveHoleCommand
  
  → Fillet удалилась, Hole вернулась на старую позицию

НАЖАЛИ UNDO ЕЩЕ РАЗ:
  stack: [CreatePadCommand, MoveHoleCommand, AddFilletCommand]
  pointer: 0  ◄── CreatePadCommand
  
  → Hole скрыулась, остался только Pad

НАЖАЛИ REDO:
  stack: [CreatePadCommand, MoveHoleCommand, AddFilletCommand]
  pointer: 1  ◄── MoveHoleCommand
  
  → Hole вернулась на новую позицию

НАЖИМАЕМ НА ЧТО-ТО НОВОЕ (например Rotate):
  stack: [CreatePadCommand, MoveHoleCommand, RotateCommand]
  pointer: 2  ◄── AddFilletCommand была удалена!
  
  → Это как в документе: если undo, потом делаешь новое действие,
     то redo история теряется (как в VS Code, Photoshop)
```

---

## 5. LOD (LEVEL OF DETAIL) СИСТЕМА

```
Сцена: 1000 панелей (20 миллионов полигонов)

Camera position: (0, 0, 500)

                      📷 Camera
                      │
                      │ distance = 50
                      ▼
        ┌──────────────────────┐
        │   HIGH QUALITY       │  LOD 0
        │   1M polygons        │  (Детальная геометрия)
        │   ├─ Panel_1: 5000   │
        │   └─ Panel_2: 5000   │
        └──────────────────────┘
                      │
                      │ distance = 150
                      ▼
        ┌──────────────────────┐
        │   MEDIUM QUALITY     │  LOD 1
        │   500K polygons      │  (Упрощенная геометрия)
        │   ├─ Panel_3: 2500   │
        │   └─ Panel_4: 2500   │
        └──────────────────────┘
                      │
                      │ distance = 300
                      ▼
        ┌──────────────────────┐
        │   LOW QUALITY        │  LOD 2
        │   100K polygons      │  (Упрощенная сильно)
        │   ├─ Panel_5: 500    │
        │   └─ Panel_6: 500    │
        └──────────────────────┘
                      │
                      │ distance = 1000+
                      ▼
        ┌──────────────────────┐
        │   ULTRA LOW          │  LOD 3
        │   10K polygons       │  (Только силуэт)
        │   ├─ Panel_7: 50     │
        │   └─ Panel_8: 50     │
        └──────────────────────┘

РЕЗУЛЬТАТ:
  Без LOD: 20M polygons → 10-15 FPS ❌
  С LOD:   2-3M polygons → 60 FPS ✅
```

---

## 6. INSTANCED MESH (много одинаковых объектов)

```
СЦЕНАРИЙ: Кухонный шкаф с 100 шурупами

BEЗ INSTANCING:
  ┌─────────────────────────────┐
  │ Screw 1 (10 polygons)       │──┐
  │ Screw 2 (10 polygons)       │  │
  │ Screw 3 (10 polygons)       │  │ 100 × DrawCall
  │ ...                          │  │
  │ Screw 100 (10 polygons)     │──┘
  └─────────────────────────────┘
  
  DrawCalls: 100
  Vertices: 1000
  Performance: 5 FPS ❌

С INSTANCING (InstancedMesh):
  ┌─────────────────────────────┐
  │ InstancedMesh {             │
  │   baseGeometry: Screw       │
  │   count: 100                │
  │   matrices: [               │  ← 100 матриц трансформ
  │     matrix1 (pos, rot),     │
  │     matrix2,                │
  │     ...                      │
  │     matrix100               │
  │   ]                         │
  │ }                           │
  └─────────────────────────────┘
  
  DrawCalls: 1
  Vertices: 10 (базовая геометрия повторяется 100 раз в GPU)
  Performance: 60 FPS ✅
```

---

## 7. SYNCHRONIZATION (Real-time Collaboration)

```
User A: "Переместил Panel_1"    User B: "Добавил Hole_1"
        │                               │
        │ WebSocket                     │ WebSocket
        └──────┬──────────────────────┬─┘
               │                      │
        ┌──────▼──────────────────────▼──────┐
        │      Server (Node.js)               │
        │  ┌────────────────────────────────┐ │
        │  │ Operation Transform Engine     │ │
        │  │ (OT or CRDT - conflict res.)  │ │
        │  │                                │ │
        │  │ "Both at same time?"           │ │
        │  │ → Apply CRDT resolution        │ │
        │  └────────────────────────────────┘ │
        │                                     │
        │  New state: {                       │
        │    Panel_1: {x: 100, y: 100},      │
        │    Hole_1: {x: 200, y: 200}        │
        │  }                                  │
        └──────┬──────────────────────────────┘
               │
        ┌──────┴──────────────────────────────┐
        │ Broadcast updated state             │
        ▼                                     ▼
    User A                               User B
    Both see the                         Both see the
    same state:                          same state:
    ├─ Panel_1 moved                     ├─ Panel_1 moved
    └─ Hole_1 added                      └─ Hole_1 added
```

---

## 8. WEB WORKER PATTERN

```
Main Thread (UI)                 Web Worker (Geometry)
    │                                  │
    │ geometryWorker.postMessage({     │
    │   type: 'simplifyGeometry',      │
    │   geometry: geom,                │
    │   ratio: 0.2                     │
    │ })                               │
    │                                  │
    │────────────────────────────────►│
    │                                  │ Calculating...
    │                                  │ (doesn't block UI!)
    │                                  │
    │ [User can still drag, click]      │ ...computing...
    │                                  │
    │ [No freezing!]                   │
    │                                  │
    │◄──────────────────────────────────│
    │ postMessage({                     │
    │   type: 'simplifyGeometryResult', │
    │   result: simplifiedGeom          │
    │ })                                │
    │                                  │
    ▼                                  ▼
Update Three.js with           [Worker waits for
new geometry                    next message]
Render frame
```

**Результат:**
- Без Web Worker: сложные расчеты → UI зависает на 3-5 сек ❌
- С Web Worker: расчеты в фоне → UI всегда отзывчив ✅

---

## 9. FULL ARCHITECTURE DIAGRAM

```
┌─────────────────────────────────────────────────────────────────────┐
│                          FRONTEND (React)                            │
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐        │
│  │   Properties   │  │  3D Viewport   │  │  Feature Tree  │        │
│  │     Panel      │  │  (Three.js)    │  │   View         │        │
│  └────────┬───────┘  └────────┬───────┘  └────────┬───────┘        │
│           │                   │                   │                 │
│           └───────────────────┼───────────────────┘                 │
│                               │                                      │
│                      ┌────────▼────────┐                            │
│                      │ Zustand Store   │                            │
│                      │ (CADDocument,   │                            │
│                      │  panels, etc)   │                            │
│                      └────────┬────────┘                            │
│                               │                                      │
│                      ┌────────▼────────┐                            │
│                      │ Command History │                            │
│                      │ (Undo/Redo)     │                            │
│                      └────────┬────────┘                            │
│                               │                                      │
└───────────────────────────────┼──────────────────────────────────────┘
                                │
                    ┌───────────▼────────────┐
                    │  Web Worker Pool       │
                    │  (Geometry calcs)      │
                    └───────────┬────────────┘
                                │
                    ┌───────────▼────────────┐
                    │  Three.js Renderer     │
                    │  (with LOD, Culling)   │
                    └───────────┬────────────┘
                                │
                    ┌───────────▼────────────┐
                    │  WebGL / Canvas        │
                    └────────────────────────┘
                                │
                    ┌───────────▼────────────┐
                    │  GPU / Display         │
                    └────────────────────────┘

BACKEND (optional):

┌──────────────────────────────────────────────────────────────────────┐
│  WebSocket Server (Node.js + Socket.io)                              │
│  ├── Operation Transform / CRDT                                      │
│  ├── User Management                                                 │
│  └── Document Persistence                                           │
│                                                                      │
│  Database (MongoDB / PostgreSQL)                                     │
│  ├── Documents metadata                                              │
│  ├── Feature Tree                                                   │
│  ├── User data                                                       │
│  └── Version history                                                 │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 10. PERFORMANCE COMPARISON

```
Scenario: 1000 панелей, 20M полигонов

WITHOUT OPTIMIZATIONS:
  ┌─────────────────────────────────────┐
  │ Frame Time: 200ms                   │
  │ FPS: 5 ❌                            │
  │ Visible: 60% объектов               │
  │ Memory: 500MB                       │
  └─────────────────────────────────────┘

WITH LOD ONLY:
  ┌─────────────────────────────────────┐
  │ Frame Time: 50ms                    │
  │ FPS: 20 ✅                           │
  │ Visible: 60% объектов               │
  │ Memory: 500MB                       │
  │ Polygons rendered: 5M (5x reduce)   │
  └─────────────────────────────────────┘

WITH LOD + CULLING:
  ┌─────────────────────────────────────┐
  │ Frame Time: 30ms                    │
  │ FPS: 33 ✅✅                         │
  │ Visible: 20% объектов (rest culled) │
  │ Memory: 500MB                       │
  │ Polygons rendered: 1M (20x reduce)  │
  └─────────────────────────────────────┘

WITH LOD + CULLING + INSTANCING:
  ┌─────────────────────────────────────┐
  │ Frame Time: 16ms                    │
  │ FPS: 60+ ✅✅✅                      │
  │ Visible: 20% объектов               │
  │ Memory: 250MB (fewer meshes)         │
  │ DrawCalls: 50 (vs 1000 without opt) │
  │ Polygons rendered: 1M               │
  └─────────────────────────────────────┘
```

---

## ИТОГОВЫЕ РЕКОМЕНДАЦИИ

```
┌────────────────────────────────────────────────────────┐
│  НЕМЕДЛЕННАЯ РЕАЛИЗАЦИЯ (сегодня-завтра)              │
│                                                        │
│  ✅ Command History (Undo/Redo)                        │
│  ✅ Feature Tree UI                                    │
│  ✅ Keyboard shortcuts (Ctrl+Z, Ctrl+Y)               │
│  ✅ LocalStorage save                                 │
│                                                        │
│  Время: 4-6 часов                                     │
│  Эффект: 🎉🎉🎉                                        │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  НЕДЕЛЯ 1 РЕАЛИЗАЦИЯ                                   │
│                                                        │
│  ✅ LOD System (дальние объекты упрощены)             │
│  ✅ Frustum Culling (не рисуем невидимое)             │
│  ✅ Performance profiling                              │
│                                                        │
│  Время: 2-3 дня                                        │
│  Эффект: FPS 5→30                                     │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  НЕДЕЛЯ 2 РЕАЛИЗАЦИЯ                                   │
│                                                        │
│  ✅ Web Worker для тяжелых расчетов                    │
│  ✅ Boolean операции (если нужны)                      │
│  ✅ Mesh simplification                                │
│                                                        │
│  Время: 2-3 дня                                        │
│  Эффект: UI никогда не зависает                       │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  НЕДЕЛЯ 3 РЕАЛИЗАЦИЯ                                   │
│                                                        │
│  ✅ WebSocket для real-time sync                       │
│  ✅ CRDT для конфликтов                                │
│  ✅ Multi-user editing                                 │
│                                                        │
│  Время: 3-4 дня                                        │
│  Эффект: Google Docs для CAD!                          │
└────────────────────────────────────────────────────────┘
```

---

✅ Все диаграммы готовы к использованию!
