# ✅ NEURAL CAD - CHECKLIST

## 📋 ДО ОБУЧЕНИЯ

### 1. Проверить окружение

```bash
# Python установлен?
python --version
# Ожидается: Python 3.8+

# pip установлен?
pip --version
# Ожидается: pip 20.0+

# Место на диске достаточно? (нужно ~2GB)
df -h

# GPU доступна? (опционально, но ускорит 10x)
python -c "import torch; print('GPU:', torch.cuda.is_available())"
```

### 2. Создать необходимые папки

```bash
cd /mnt/projects/другие\ проекты/базис-веб

# Папки для моделей
mkdir -p models
mkdir -p public/models
mkdir -p logs

# Проверить структуру
ls -la scripts/
# должен быть: train_neural_cad.py

ls -la requirements-neural.txt
# должен быть файл
```

### 3. Установить зависимости

```bash
# Установить Python пакеты
pip install -r requirements-neural.txt

# Проверить torch
python -c "import torch; print('PyTorch version:', torch.__version__)"

# Проверить numpy
python -c "import numpy; print('NumPy version:', numpy.__version__)"

# Проверить onnx
python -c "import onnx; print('ONNX version:', onnx.__version__)"
```

### 4. Запустить тест датасета (опционально)

```bash
# Быстрый тест генерации данных (2 минуты)
python -c "
from scripts.train_neural_cad import FurnitureDatasetGenerator
gen = FurnitureDatasetGenerator()
dataset = gen.generate_dataset(num_samples=100)  # 100 примеров для теста
print(f'✅ Dataset OK: {len(dataset)} examples')
print(f'   Shape: {dataset[0][0].shape}, {dataset[0][1].shape}')
"
```

---

## 🚀 ОБУЧЕНИЕ

### Этап 1: Запустить обучение

```bash
# Основная команда
python scripts/train_neural_cad.py

# Результаты в реальном времени:
# [1/4] Генерация синтетического датасета...
# [2/4] Инициализация модели...
# [3/4] Обучение модели (это займёт 1-2 часа)...
# [4/4] Экспорт модели в ONNX...
```

### Этап 2: Мониторить прогресс

```bash
# В отдельном терминале следить за лог-файлом
tail -f logs/training.log

# Ожидаемый вывод:
# Epoch 1/50 | Train Loss: 0.354 | Val Loss: 0.312
# Epoch 2/50 | Train Loss: 0.298 | Val Loss: 0.289
# ...
# Epoch 50/50 | Train Loss: 0.051 | Val Loss: 0.048
```

### Этап 3: Проверить результаты

```bash
# После завершения должны быть файлы:
ls -lh models/
# -rw-r--r-- 50M furniture-encoder-v1.onnx
# -rw-r--r-- 50M furniture-decoder-v1.onnx
# -rw-r--r-- 1K  metadata.json

# Проверить metadata
cat models/metadata.json
# {
#   "model_name": "NeuralCADModel",
#   "version": "2.1.0",
#   "training_data_size": 5000,
#   "accuracy": 0.953,
#   "inference_time_ms": 2100,
#   "parameter_means": [1000, 1400, 600, ...],
#   "parameter_stds": [400, 500, 200, ...]
# }
```

---

## 📦 РАЗВЁРТЫВАНИЕ

### Этап 1: Скопировать модели в браузер

```bash
# Скопировать в публичную папку
cp models/*.onnx public/models/
cp models/metadata.json public/models/

# Проверить
ls -lah public/models/
# должны быть *.onnx и metadata.json

# Файлы должны быть доступны по http://localhost:3000/models/
```

### Этап 2: Проверить файлы React компонентов

```bash
# Проверить наличие файлов
ls services/cad/NeuralCADGenerator.ts
# должен быть ✅

ls components/NeuralGenerationPanel.tsx
# должен быть ✅
```

### Этап 3: Интегрировать в App.tsx

```bash
# Вариант 1: Заменить файл (если хотите быстро)
mv App.tsx App.original.tsx
cp AppWithNeural.tsx App.tsx

# Вариант 2: Ручная интеграция (см. NEURAL_INTEGRATION_GUIDE.md)
```

### Этап 4: Установить npm зависимости (если изменились)

```bash
npm install
npm run build

# Проверить ошибки
npm run typecheck
# должны быть 0 ошибок
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Локальный запуск (5 минут)

```bash
# Запустить dev сервер
npm run dev

# Браузер должен открыться на http://localhost:3000
# Проверить:
# ✅ Страница загружается без ошибок
# ✅ Консоль браузера чистая (F12)
# ✅ Нет 404 ошибок для models/
```

### Тест 2: Проверить загрузку моделей (2 минуты)

```bash
# Открыть консоль браузера (F12)

# Команда 1: Проверить, загружены ли модели
window.__neuralCADGenerator?.isReady()
# Ожидается: true (или будет загружаться)

# Подождать 5 сек...

# Команда 2: Проверить метаданные
window.__neuralCADGenerator?.getStatus()
# Ожидается:
# {
#   name: "NeuralCADModel",
#   version: "2.1.0",
#   accuracy: 0.953,
#   trainingDataSize: 5000,
#   ...
# }
```

### Тест 3: Генерировать пример (3 минуты)

```bash
# Клавиша Ctrl+N для открытия Neural Generator
# Или нажать кнопку "✨ Neural Gen" в левой панели

# В UI:
# ✅ Видеть параметры слайдеры
# ✅ Видеть кнопку "✨ Сгенерировать 3D"
# ✅ Нажать кнопку
# ✅ Дождаться 1-3 сек
# ✅ Видеть статистику:
#    - Vertices: XXXX
#    - Faces: XXXX
#    - Generation time: XXXms
#    - Confidence: XX.X%
```

### Тест 4: Проверить консоль браузера

```javascript
// Консоль браузера (F12) должна показать:
✅ [NeuralCADGenerator] Models loaded
✅ [NeuralCADGenerator] Generation completed: 5000 vertices, 8000 faces

// НЕ должно быть:
❌ Failed to load models
❌ WASM not available
❌ TypeError: Cannot read property...
```

---

## 🎯 ПАРАМЕТРЫ ПРОВЕРКИ

### Качество генерации

```
Параметр         | Min   | Max   | Default | Status
===============================================
width            | 300   | 3000  | 1200    | ✅
height           | 400   | 2500  | 1400    | ✅
depth            | 300   | 1000  | 600     | ✅
shelfCount       | 0     | 10    | 3       | ✅
shelfThickness   | 4     | 25    | 16      | ✅
edgeType         | 0     | 2     | 1       | ✅
materialDensity  | 600   | 1200  | 800     | ✅
```

### Ожидаемые результаты

```
Метрика               | Ожидаемое  | Статус
====================================================
Accuracy              | >95%       | ✅ Проверить
Inference time        | 1-3 sec    | ✅ Проверить
Memory usage          | 128-256MB  | ✅ Проверить
Vertices per model    | 5000       | ✅ Проверить
Faces per model       | 8000       | ✅ Проверить
Confidence score      | 0.85-0.99  | ✅ Проверить
Deterministic output  | Yes        | ✅ Проверить
```

---

## 🐛 TROUBLESHOOTING

### Проблема: "Failed to load models"

```bash
# Решение 1: Проверить файлы
ls -lh public/models/
# Должны быть *.onnx файлы

# Решение 2: Проверить права доступа
chmod 644 public/models/*

# Решение 3: Очистить кэш браузера
# Ctrl+Shift+Delete → Очистить всё
```

### Проблема: "WASM not available"

```bash
# Решение: Обновить ONNX Runtime
npm install onnxruntime-web@latest

# Или переустановить
npm uninstall onnxruntime-web
npm install onnxruntime-web
```

### Проблема: "Out of memory"

```bash
# Решение 1: Уменьшить batch size в train_neural_cad.py
# BATCH_SIZE = 16  # вместо 32

# Решение 2: Перезагрузить браузер (F5)

# Решение 3: Проверить другие открытые вкладки/приложения
```

### Проблема: "Model inference takes >5 seconds"

```bash
# Решение 1: Использовать GPU для обучения
# (уже учтено в скрипте автоматически)

# Решение 2: Уменьшить quality параметр
# quality: 0.7  # вместо 0.9 (быстрее ~2x)

# Решение 3: Кэшировать результаты
# const cache = new Map();
```

### Проблема: "Accuracy не достигает 95%"

```bash
# Решение 1: Увеличить epochs
# epochs=100  # вместо 50

# Решение 2: Увеличить датасет
# num_samples=10000  # вместо 5000

# Решение 3: Уменьшить learning rate
# lr=1e-4  # вместо 1e-3
```

---

## ✨ ФИНАЛЬНАЯ ПРОВЕРКА

### Перед продакшеном

```bash
# 1. Проверить файлы моделей
ls -lh public/models/
# ✅ Все файлы присутствуют

# 2. Проверить размер моделей
du -sh public/models/
# Ожидается: ~100MB (для обеих моделей)

# 3. Проверить git статус
git status
# ✅ models/ и public/models/ в .gitignore

# 4. Запустить тесты
npm run test

# 5. Билд для продакшена
npm run build
# ✅ Build successful

# 6. Проверить бандл
ls -lh dist/
# Проверить размер (не должен сильно увеличиться)
```

### Запустить в продакшене

```bash
# 1. Собрать приложение
npm run build

# 2. Запустить сервер
npm run preview
# или развернуть dist/ на хостинг

# 3. Проверить в браузере
# http://localhost:4173  (preview)
# или http://yoursite.com (production)

# 4. Проверить загрузку моделей
# Консоль браузера (F12):
window.__neuralCADGenerator?.isReady()
# ✅ должно быть true
```

---

## 📊 СТАТИСТИКА

### Время выполнения

```
Этап                    | Время    | Status
================================================
1. Подготовка окружения | 5 мин    | ✅
2. Установка deps       | 10 мин   | ✅
3. Обучение модели      | 1-2 часа | ✅
4. Развёртывание        | 5 мин    | ✅
5. Тестирование         | 5 мин    | ✅
================================================
ИТОГО                   | ~2.5 часа| ✅
```

### Размеры файлов

```
Файл                           | Размер   | Status
====================================================
furniture-encoder-v1.onnx      | ~50MB    | ✅
furniture-decoder-v1.onnx      | ~50MB    | ✅
metadata.json                  | 1KB      | ✅
NeuralCADGenerator.ts          | 1.1MB    | ✅
NeuralGenerationPanel.tsx      | 400KB    | ✅
train_neural_cad.py            | 800KB    | ✅
```

### Требования

```
Требование        | Минимум   | Рекомендуемо | Статус
===================================================
RAM               | 4GB       | 8GB          | ✅
Место на диске    | 2GB       | 5GB          | ✅
Python версия     | 3.8       | 3.10+        | ✅
Node.js версия    | 14        | 18+          | ✅
Время обучения    | -         | 1-2 часа     | ✅
```

---

**🎉 ГОТОВО К ЗАПУСКУ!**

Начните с: `python scripts/train_neural_cad.py`
