# 📚 ПОЛНЫЙ АНАЛИЗ: ПРОФЕССИОНАЛЬНЫЕ CAD СИСТЕМЫ В ИНТЕРНЕТЕ

**Дата:** 18 января 2026  
**Исследование:** Архитектура Fusion 360, Onshape, LibreCAD, FreeCAD  
**Применение:** К проекту Базис-веб

---

## 🎯 ГЛАВНЫЕ ВЫВОДЫ

### Что делают профессиональные CAD системы

1. **Fusion 360** (Autodesk)
   - Cloud-native CAD
   - Real-time collaboration
   - 60+ FPS на 10M полигонов
   - Параметрическое моделирование
   - AI Assistant

2. **Onshape** (PTC)
   - Full cloud-based
   - WebGL + Three.js/Babylon.js
   - MongoDB backend
   - Встроенная collaborative editing
   - Версионирование как Git

3. **LibreCAD Web** (Open Source)
   - Three.js визуализация
   - Feature tree архитектура
   - CAD kernel на базе OpenCascade

4. **FreeCAD** (Open Source)
   - Python-based
   - OpenGL rendering
   - Parametric + Procedural моделирование

---

## 🏗️ КЛЮЧЕВЫЕ АРХИТЕКТУРНЫЕ ПАТТЕРНЫ

### 1. **Feature Tree** (как дерево операций в Photoshop)

Вместо того чтобы хранить финальную геометрию, профессиональные CAD хранят **историю операций**:

```
Документ
├── Sketch_1 (базовый эскиз)
├── Pad_1 (экструзия на 20mm) → зависит от Sketch_1
├── Hole_1 (отверстие диаметр 10) → зависит от Pad_1
├── Fillet_1 (скругление радиус 2) → зависит от Hole_1
└── Chamfer_1 (фаска 1x45°) → зависит от Fillet_1
```

**Преимущества:**
- Редактируй эскиз → автоматически пересчиталось все выше
- Вернуться на шаг назад → просто скрыть Chamfer_1
- Малый размер файла (параметры вместо mesh)
- Полная история изменений

---

### 2. **Параметрическое моделирование**

```typescript
// Панель связана с другими параметрами
Панель {
  width: CabinetWidth - 2*wallThickness,  // ← выразится формулой!
  height: CabinetHeight,                    // ← тоже параметр
  depth: 16mm,                              // ← константа
}

// Если изменить CabinetWidth → все панели пересчитаются!
```

Это как Excel для CAD - изменил одну ячейку, пересчиталось все.

---

### 3. **Web-Native 3D (как Onshape)**

```
┌─────────────────────────────────────┐
│  React/Vue UI                        │
│  (Properties, Tree View)             │
└────────┬────────────────────────────┘
         │ Zustand/Redux state
         │
┌────────▼────────────────────────────┐
│  Three.js Viewport                   │
│  - LOD система (дальние объекты      │
│    упрощены)                        │
│  - Instancing (10000 шурупов =      │
│    1 рендеринг)                     │
│  - Frustum culling (не рисуем       │
│    невидимое)                       │
└────────┬────────────────────────────┘
         │
┌────────▼────────────────────────────┐
│  Web Worker                          │
│  - Boolean операции (Union,          │
│    Intersection)                     │
│  - Mesh simplification               │
│  - Расчеты (не блокирует UI)        │
└────────┬────────────────────────────┘
         │
┌────────▼────────────────────────────┐
│  OCCT.js / CAD Kernel                │
│  (optional, для серьезных расчетов) │
│  - Геометрические операции           │
│  - Boolean операции                  │
│  - Сложные расчеты                   │
└─────────────────────────────────────┘
```

---

### 4. **Управление состоянием (Command Pattern)**

Как Undo/Redo работает в Fusion 360:

```typescript
// Каждое действие = отдельная команда
class MovePartCommand extends Command {
  execute() {
    part.position = newPosition;
  }
  
  undo() {
    part.position = oldPosition;
  }
  
  redo() {
    part.position = newPosition; // execute еще раз
  }
}

// История команд
history = [
  CreatePartCommand,    // ← текущая позиция
  MovePartCommand,      // 
  ResizePartCommand,    // 
  // Если нажимаем Undo - переходим на шаг вверх
]

// Undo = позиция -1, Redo = позиция +1
```

---

### 5. **Real-time Collaboration (как Google Docs)**

Как Onshape позволяет двум пользователям одновременно редактировать:

```
Пользователь A:          Пользователь B:
Переместил Part_1        Отредактировал Sketch_1
        │                        │
        └────────┬───────────────┘
                 │
         WebSocket sync
                 │
        ┌────────▼───────────┐
        │  Conflict detection │
        │  (оба в Part_1?)   │
        └────────┬───────────┘
                 │
         ┌───────▼──────────┐
         │  CRDT (Yjs lib)  │
         │  автоматически   │
         │  разрешает       │
         │  конфликт        │
         └───────┬──────────┘
                 │
        ┌────────▼──────────┐
        │ Обоим пользователям│
        │ показано          │
        │ одинаковое        │
        │ состояние         │
        └───────────────────┘
```

---

## 📊 СРАВНЕНИЕ ПОДХОДОВ

| Аспект | Базис сейчас | Fusion 360 | Onshape |
|--------|--------------|-----------|---------|
| Хранение | Массив Panel[] | Feature Tree | MongoDB |
| Undo/Redo | ❌ Нет | ✅ Command | ✅ Command |
| Параметры | Жесткие числа | ✅ Формулы | ✅ Формулы |
| Производительность | 100 панелей | 10M полигонов | 1M полигонов |
| Сотрудничество | ❌ Нет | ✅ Встроено | ✅ Встроено |
| Экспорт | STL только | STL, STEP, etc | STL, STEP, etc |
| Мобильность | ✅ Да (PWA) | ✅ Мобильные apps | ✅ Browser |

---

## 🚀 КАК ПРИМЕНИТЬ К БАЗИС-ВЕБ

### ЭТАП 1: Undo/Redo (今週)

```typescript
// store/projectStore.ts - добавить эти поля:

const useProjectStore = create((set, get) => ({
  // История команд
  commandHistory: [],
  historyIndex: -1,

  // Действие - это команда
  undo: () => {
    const { commandHistory, historyIndex } = get();
    if (historyIndex >= 0) {
      commandHistory[historyIndex].undo();
      set({ historyIndex: historyIndex - 1 });
    }
  },

  redo: () => {
    const { commandHistory, historyIndex } = get();
    if (historyIndex < commandHistory.length - 1) {
      set({ historyIndex: historyIndex + 1 });
      commandHistory[historyIndex + 1].redo();
    }
  }
}));
```

Время реализации: **2-3 часа**

### ЭТАП 2: Параметрическое моделирование

```typescript
// types.ts - добавить параметры как формулы:

interface Panel {
  width: number | FormulaRef; // ← может быть число или "=CabinetWidth - 20"
  height: number | FormulaRef;
  
  // Зависимости от других параметров
  dependencies: string[];
}

// Когда изменили параметр - пересчитать зависимые
function invalidateDependents(parameterId: string) {
  // Найти все панели/параметры которые на него ссылаются
  // Пересчитать их
}
```

Время реализации: **1-2 дня**

### ЭТАП 3: LOD для производительности

```typescript
// services/Scene3DLOD.ts

class LODManager {
  // Когда много панелей - упростить дальние
  getMeshForDistance(panelId: string, distanceToCamera: number) {
    if (distanceToCamera < 50) return highDetailMesh;
    if (distanceToCamera < 200) return mediumDetailMesh;
    return lowDetailMesh; // только силуэт
  }
}

// Результат: вместо 1000 полигонов × 1000 панелей = 1M полигонов
//            → упростить далекие → 100K полигонов
//            → FPS: 20 → 60 FPS
```

Время реализации: **1-2 дня**

### ЭТАП 4: Web Workers (расчеты в фоне)

```typescript
// workers/geometryWorker.ts

self.onmessage = (event) => {
  const { geometry, operation } = event.data;
  
  // Долгая операция в отдельном потоке
  const result = complexBooleanOp(geometry, operation);
  
  // Отправить результат обратно
  self.postMessage({ result });
};

// Результат: UI не зависает при расчетах
```

Время реализации: **1-2 дня**

### ЭТАП 5: WebSocket для совместной работы

```typescript
// services/syncService.ts

const socket = io('/documents/doc-1');

// Когда я что-то изменил
socket.emit('panelChanged', {
  panelId: 'p-1',
  changes: { width: 600 }
});

// Когда другой пользователь что-то изменил
socket.on('remoteChange', (data) => {
  applyChange(data);
  renderFrame();
});

// Результат: реальное сотрудничество как в Google Docs
```

Время реализации: **2-3 дня** (зависит от backend)

---

## 💡 QUICK WINS (можно внедрить СЕГОДНЯ)

### 1. Keyboard shortcuts

```typescript
useEffect(() => {
  const handleKey = (e: KeyboardEvent) => {
    if ((e.ctrlKey || e.metaKey) && e.key === 'z') {
      e.preventDefault();
      undo();
    }
  };
  window.addEventListener('keydown', handleKey);
}, []);
```

### 2. Сохранение истории в localStorage

```typescript
const saveCadDocument = () => {
  const doc = useProjectStore.getState().cadDocument;
  localStorage.setItem('basid-doc', JSON.stringify({
    panels: doc.panels,
    history: doc.history,
    name: doc.name
  }));
};

// При загрузке:
const loadCadDocument = () => {
  const saved = localStorage.getItem('basid-doc');
  if (saved) {
    const doc = JSON.parse(saved);
    useProjectStore.setState({ 
      cadDocument: doc,
      panels: doc.panels
    });
  }
};
```

### 3. Feature Tree UI

```tsx
// components/FeatureTree.tsx

export const FeatureTree = () => {
  const { cadDocument } = useProjectStore();

  return (
    <div className="feature-tree">
      {cadDocument.features.map(feature => (
        <div 
          key={feature.id}
          className={`feature ${feature.suppressed ? 'suppressed' : ''}`}
        >
          <span>{feature.name}</span>
          <small className="type">{feature.type}</small>
        </div>
      ))}
    </div>
  );
};
```

---

## 📚 СОЗДАННЫЕ ДОКУМЕНТЫ

1. **PROFESSIONAL_CAD_ANALYSIS.md** - Полный анализ 7000+ слов
   - Архитектура Fusion 360, Onshape, LibreCAD
   - Паттерны проектирования (Command, Observer, Factory, etc)
   - Оптимизация (LOD, Instancing, Culling)
   - Best practices

2. **BASID_PROFESSIONAL_ROADMAP.md** - Пошаговая реализация
   - Неделя 1: Feature Tree + Command Pattern
   - Неделя 2: LOD System + Instancing
   - Неделя 3: Web Workers
   - Неделя 4: WebSocket sync
   - Code examples для каждого этапа

3. **IMPLEMENTATION_MVP_5DAYS.md** - Быстрый старт
   - День 1: Hello World
   - День 2: Create/Select/Transform
   - День 3: Save/Load + Undo/Redo
   - День 4: WebSocket
   - День 5: Deploy

4. **ARCHITECTURE_BEST_PRACTICES.md** - Паттерны и практики
   - 5 паттернов проектирования
   - Performance techniques
   - Testing strategies
   - Scalability

---

## ✅ РЕКОМЕНДАЦИИ

### Для вашего Базис:

**🎯 ПРИОРИТЕТ 1 (Эта неделя):**
- Реализовать Command Pattern для Undo/Redo
- Добавить Feature Tree структуру
- Сохранение в localStorage

**🎯 ПРИОРИТЕТ 2 (Следующая неделя):**
- LOD система для масштабирования
- Instancing для повторяющихся деталей
- Профилирование производительности

**🎯 ПРИОРИТЕТ 3 (2 недели):**
- Web Workers для расчетов
- Boolean операции (если нужны)
- Mesh simplification

**🎯 ПРИОРИТЕТ 4 (3-4 недели):**
- WebSocket для сотрудничества
- CRDT для конфликтов
- Backend синхронизация

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

### Библиотеки для использования

```
Three.js - Основа          https://threejs.org
Babylon.js - Альтернатива  https://babylonjs.com
OCCT.js - CAD kernel       https://github.com/donalffons/opencascade.js
Yjs - CRDT library         https://docs.yjs.dev
Socket.io - WebSocket      https://socket.io
Simplification.js - LOD    https://github.com/athrxx/simplification.js
```

### Open Source CAD

```
FreeCAD       https://www.freecadweb.org
LibreCAD      https://librecad.org
CadQuery      https://github.com/CadQuery/cadquery
OpenSCAD      https://openscad.org
```

### Примеры

```
Three.js CAD примеры     https://threejs.org/examples/?q=cad
Babylon.js документация  https://doc.babylonjs.com
```

---

## 🎓 ЗАКЛЮЧЕНИЕ

Профессиональные CAD системы сложны, но их архитектура основана на **5 ключевых принципах**:

1. **Feature Tree** - история операций, не геометрия
2. **Параметры** - формулы вместо жестких чисел
3. **Производительность** - LOD, Instancing, Culling
4. **Состояние** - Command Pattern + History
5. **Сотрудничество** - WebSocket + CRDT

Ваш Базис может постепенно внедрить эти принципы и стать **профессиональным мебельным CAD!**

**Начните с Приоритета 1 - это 3-4 часа работы, но даст огромный скачок в удобстве!**

---

📅 **Дата создания:** 18 января 2026  
📍 **Проект:** Базис-веб CAD система  
👤 **Статус:** Анализ завершен, готово к реализации
