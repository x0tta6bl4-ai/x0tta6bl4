# ⚡ БЫСТРЫЙ СТАРТ (30 СЕКУНД)

## 🎯 Выберите вашу операционную систему

### Windows

**Способ 1 (Самый простой):**
```
Двойной клик на файл → start.bat
```

**Способ 2 (PowerShell):**
```powershell
cd "путь\к\проекту\basis-web"
.\start.bat
```

**Способ 3 (cmd):**
```cmd
cd путь\к\проекту\basis-web
start.bat
```

### macOS / Linux

**Способ 1 (Рекомендуется):**
```bash
cd /path/to/basis-web
./start.sh
```

**Способ 2 (Если скрипт не запускается):**
```bash
cd /path/to/basis-web
chmod +x start.sh
./start.sh
```

**Способ 3 (Ручной запуск):**
```bash
cd /path/to/basis-web
npm install  # Первый раз только
npm run dev
```

---

## ✅ Проверка успешного запуска

Вы должны увидеть в терминале:
```
VITE v6.2.0 ready in 234 ms

➜  Local:   http://localhost:5173/
➜  press h to show help
```

Затем откройте браузер на адресе: **http://localhost:5173/**

---

## ⚙️ Требования

- **Node.js 18+** (https://nodejs.org/)
- Интернет-соединение
- Браузер (Chrome, Firefox, Safari, Edge)

---

## 🚨 Если не работает

### Ошибка: "node: command not found"
→ Node.js не установлен. Скачайте https://nodejs.org/

### Ошибка: "Port 5173 already in use"
→ Другое приложение использует порт. Закройте его или:
```bash
npm run dev -- --port 5174
```

### Ничего не происходит
1. Убедитесь, что вы в правильной папке
2. Удалите node_modules: `rm -rf node_modules`
3. Переустановите: `npm install`
4. Запустите снова

---

## 📖 Полная документация

Если нужны подробности: **→ [SETUP_LOCAL.md](./SETUP_LOCAL.md)**

---

## 🎉 Готово!

Приложение загружается... вы готовы проектировать мебель! 🚀
