# 🚀 Руководство: Локальный запуск BASIS-WEB

## 📋 Требования

### Минимальные требования
- **Node.js** версия 18+ (рекомендуется 20+)
- **npm** версия 9+ (идёт с Node.js)
- **1 GB** свободного места на диске
- Интернет-соединение (для загрузки зависимостей)

### Дополнительно (опционально)
- **Gemini API Key** (для AI функций) - получить на https://aistudio.google.com/apikey
- **VS Code** или любой текстовый редактор

---

## 📥 Шаг 1: Установка Node.js

### Windows
1. Перейдите на https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуется)
3. Запустите установщик (.msi файл)
4. Следуйте инструкциям установщика
5. После установки перезагрузитесь

### macOS
```bash
# Через Homebrew (рекомендуется)
brew install node

# Или скачайте с https://nodejs.org/
```

### Linux (Ubuntu/Debian)
```bash
# Через apt
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### Linux (Fedora)
```bash
sudo dnf install nodejs npm
```

---

## 🔍 Шаг 2: Проверка установки

```bash
# Проверить версию Node.js
node -v
# Должно вывести: v20.x.x или выше

# Проверить версию npm
npm -v
# Должно вывести: 9.x.x или выше
```

---

## 📦 Шаг 3: Подготовка проекта

### Откройте терминал/командную строку

**Windows:**
- Откройте папку проекта
- Нажмите Shift + правый клик → "Открыть здесь окно PowerShell"
- Или откройте `cmd.exe` и перейдите в папку

**macOS/Linux:**
```bash
cd /path/to/basis-web
```

### Установка зависимостей

```bash
npm install
```

Это может занять 1-3 минуты в первый раз.

---

## 🔑 Шаг 4: Настройка переменных окружения

### Способ 1: Через GUI (легче)

1. Откройте файл `.env.local` в текстовом редакторе
2. Замените строку:
```
VITE_GEMINI_API_KEY=PLACEHOLDER_API_KEY
```

На вашу реальный ключ (опционально):
```
VITE_GEMINI_API_KEY=ваш_ключ_gemini_здесь
```

3. Сохраните файл (Ctrl+S)

### Способ 2: Через командную строку

**Windows:**
```powershell
# Установить переменную среды (на сеанс)
$env:VITE_GEMINI_API_KEY = "ваш_ключ_здесь"
```

**macOS/Linux:**
```bash
export VITE_GEMINI_API_KEY="ваш_ключ_здесь"
```

### Получить Gemini API Key

1. Перейдите на https://aistudio.google.com/apikey
2. Нажмите "Create API key"
3. Скопируйте ключ
4. Вставьте в `.env.local`

**Важно:** Без ключа AI функции работать не будут, но приложение запустится.

---

## 🚀 Шаг 5: Запуск приложения

### Способ 1: Использовать скрипты (рекомендуется)

**Windows:**
```bash
# Просто двойной клик на файл start.bat
# ИЛИ в PowerShell:
.\start.bat
```

**macOS/Linux:**
```bash
# Сделать скрипт исполняемым
chmod +x start.sh

# Запустить
./start.sh
```

### Способ 2: Запустить npm напрямую

```bash
npm run dev
```

---

## ✅ Проверка запуска

Если всё хорошо, вы должны увидеть:

```
  VITE v6.2.0  ready in 234 ms

  ➜  Local:   http://localhost:5173/
  ➜  press h to show help
```

---

## 🌐 Открытие приложения

1. Откройте веб-браузер (Chrome, Firefox, Safari, Edge)
2. Перейдите на **http://localhost:5173/**
3. Приложение должно загрузиться 🎉

---

## 🛠️ Доступные команды

```bash
# Запуск dev сервера (горячая перезагрузка)
npm run dev

# Собрать для production
npm run build

# Предпросмотр production сборки
npm run preview

# Проверить TypeScript
npx tsc --noEmit
```

---

## 📊 Структура папок в проекте

```
basis-web/
├── components/          ← React компоненты
│   ├── Scene3D.tsx     ← 3D сцена (Three.js)
│   ├── EditorPanel.tsx ← Редактор панелей
│   ├── CutList.tsx     ← Список кроя
│   ├── DrawingView.tsx ← 2D чертежи
│   └── ...
├── services/           ← Бизнес-логика
│   ├── geminiService.ts    ← AI функции
│   └── hardwareUtils.ts    ← Работа с крепежом
├── App.tsx            ← Главный компонент
├── index.tsx          ← Точка входа
├── types.ts           ← TypeScript типы
├── package.json       ← Зависимости
├── vite.config.ts     ← Конфиг Vite
└── .env.local         ← Переменные окружения
```

---

## 🐛 Решение проблем

### Проблема: "npm: command not found"

**Решение:**
1. Node.js не установлен или не добавлен в PATH
2. Перезагрузитесь после установки Node.js
3. На Windows переустановите Node.js, отметив опцию "Add to PATH"

### Проблема: "EACCES: permission denied"

**Решение (Linux/macOS):**
```bash
chmod +x start.sh
sudo chown -R $USER ~/.npm
```

### Проблема: "Port 5173 already in use"

**Решение:**
- Закройте другое приложение, использующее порт 5173
- Или используйте другой порт:
```bash
npm run dev -- --port 5174
```

### Проблема: "Cannot find module '@google/genai'"

**Решение:**
```bash
# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Приложение загружается, но падает

**Решение:**
1. Откройте Developer Tools (F12)
2. Смотрите Console tab на ошибки
3. Убедитесь, что у вас установлена правильная версия Node.js (18+)
4. Очистите кэш браузера (Ctrl+Shift+Delete)

---

## 🎯 Первый запуск

После успешного запуска вы можете:

1. ✅ **Создавать панели** - добавлять детали мебели
2. ✅ **Редактировать свойства** - размеры, материалы, цвета
3. ✅ **Видеть 3D превью** - вращать, зумировать
4. ✅ **Расставлять крепеж** - автоматически или вручную
5. ✅ **Экспортировать** - чертежи, списки кроя
6. ✅ **Использовать AI** - генерировать дизайны (если ключ Gemini добавлен)

---

## 📱 Работа с горячей перезагрузкой

Во время разработки:
- Любые изменения в файлах **автоматически перезагружают** приложение в браузере
- Состояние приложения **сохраняется** при перезагрузке
- Вы видите ошибки **прямо в браузере**

---

## 🔧 Production сборка

Когда готовы развертывать:

```bash
# Собрать оптимизированную версию
npm run build

# Результат находится в папке dist/
# Можно загрузить на хостинг (Vercel, Netlify и т.д.)
```

---

## 📖 Дополнительная документация

- **HARDWARE_USER_GUIDE.md** - Руководство по работе с крепежом
- **HARDWARE_IMPROVEMENTS.md** - Технические улучшения
- **README.md** - Основная документация проекта
- **HARDWARE_ANALYSIS.md** - Анализ системы крепежа

---

## 💡 Советы

### Оптимизация производительности

```bash
# Используйте dev режим для разработки (быстрее)
npm run dev

# Используйте production режим для тестирования скорости
npm run build
npm run preview
```

### Подключить к своей сети

```bash
# Запустить на 0.0.0.0 (доступно из других ПК в сети)
npm run dev -- --host
```

Тогда приложение доступно по адресу: `http://<ваш_ip>:5173/`

### Отладка

Если что-то не работает:
1. Откройте DevTools (F12)
2. Смотрите Console tab на ошибки
3. Проверьте Network tab на запросы

---

## ✅ Чек-лист установки

- [ ] Node.js установлен (версия 18+)
- [ ] npm установлен и работает
- [ ] Папка проекта открыта в терминале
- [ ] Выполнен `npm install`
- [ ] Файл `.env.local` существует
- [ ] Выполнен `npm run dev`
- [ ] Приложение открывается на http://localhost:5173/
- [ ] 3D сцена загружается без ошибок

---

## 🆘 Нужна помощь?

1. **Проверьте версии:**
   ```bash
   node -v   # Должно быть 18+
   npm -v    # Должно быть 9+
   ```

2. **Проверьте логи:**
   - Откройте DevTools (F12 в браузере)
   - Смотрите Console tab

3. **Переустановите зависимости:**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

4. **Очистите браузер:**
   - Ctrl+Shift+Delete → Clear cache
   - Перезагрузите страницу (F5)

---

## 🎓 Дополнительное обучение

### Three.js (3D графика)
- https://threejs.org/docs/
- https://threejs.org/examples/

### React 19
- https://react.dev/learn

### TypeScript
- https://www.typescriptlang.org/docs/

### Vite
- https://vitejs.dev/guide/

---

**Версия:** 1.0  
**Дата обновления:** 12 января 2026  
**Статус:** Production Ready ✅

Удачи в разработке! 🚀
