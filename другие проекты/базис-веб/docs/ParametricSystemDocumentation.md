# Параметрическая система v2.1 для CAD системы «Базис-веб»

## 🎯 Описание системы

Параметрическая система v2.1 — это ядровой модуль CAD системы, обеспечивающий:
- **Параметрическое проектирование** с топологической сортировкой зависимостей
- **Реальное время** предпросмотра изменений
- **Историю версий** с функциями undo/redo и ветвлением
- **Производственные допуски** и валидацию размеров
- **Расчет себестоимости** с разбивкой по позициям
- **Интерактивное редактирование** с режимом черновика

## 📋 Основные функции

### 1. Параметрическое проектирование
- **Гибкая архитектура параметров** с поддержкой типов: числовых, строковых, логических и селект
- **Зависимости параметров** с топологической сортировкой для эффективного пересчета
- **Автоматическая валидация** с пользовательскими сообщениями об ошибках
- **Расширяемый движок параметров** для добавления новых типов

### 2. История версий
- **Сохранение и загрузка версий** проекта
- **Undo/Redo операций** с шаговой историей
- **Ветвление проекта** для параллельной разработки
- **Мердж ветвей** с автоматическим разрешением конфликтов
- **Difference analysis** между версиями

### 3. Производственные допуски
- **Система допусков** для проверки соответствия размеров
- **Отчеты о проверке** с детальной информацией по каждому параметру
- **Групповые проверки** для ускорения процесса
- **Настройки допусков** по стандартам ISO/ГОСТ

### 4. Интерактивное редактирование
- **Режим редактирования** с динамическим предпросмотром
- **Режим черновика** для быстрых изменений без сохранения
- **Настройки предпросмотра** (камера, освещение, отображение)
- **Real-time обновление** при изменении параметров

### 5. Расчет себестоимости
- **Разбивка затрат** по материалам, фурнитуре и работам
- **Добавление наценок и накладных расходов**
- **Экспорт отчетов** в форматы CSV, JSON, PDF
- **Обновление стоимости** при изменении параметров
- **Математическое моделирование** затрат

### 6. Руководство сборки
- **Генерация пошаговых инструкций**
- **Сопровождение в нескольких языках**
- **Документация инструментов и материалов**
- **Анимации и изображения** для каждого шага
- **Экспорт руководства** в HTML и PDF

## 🛠️ Архитектура системы

### Основные компоненты

#### 1. ParametricSystem
Основной сервис для управления параметрами:
- `getParameter(id)` - получение параметра по идентификатору
- `setParameter(id, value)` - обновление параметра
- `validateParameters()` - валидация всех параметров
- `recalculateAffected()` - пересчет зависимых компонентов

#### 2. CostCalculation
Модуль для расчета стоимости:
- `calculateCost()` - основной расчет
- `updateCostBreakdown()` - обновление структуры затрат
- `getCostItemsByType()` - получение затрат по типу
- `exportCostReport()` - экспорт в разные форматы

#### 3. ToleranceSystem
Система для работы с допусками:
- `addTolerance()` - добавление нового допуска
- `removeTolerance()` - удаление допуска
- `runToleranceCheck()` - запуск проверки
- `getDimensionChecks()` - получение результатов

#### 4. VersionHistory
Управление историей версий:
- `saveVersion()` - сохранение версии
- `loadVersion()` - загрузка версии
- `undo()/redo()` - операции отмены/повтора
- `createBranch()` - создание ветки
- `mergeBranch()` - слияние веток

#### 5. AssemblyGuide
Генератор руководства сборки:
- `generateAssemblyGuide()` - генерация инструкций
- `getStepDetails()` - получение деталей шага
- `exportAssemblyGuide()` - экспорт руководства

## 📊 Пример использования

```typescript
import { ParametricSystem } from './services/ParametricSystem';
import { Tolerance } from './types/ParametricSystem';

const system = new ParametricSystem();

// Управление параметрами
const widthParam = system.getParameter('width');
const updateResult = system.setParameter('width', 1300);

// Проверка параметров
const errors = system.validateParameters();
if (errors.length > 0) {
  console.error('Ошибки:', errors);
}

// Расчет стоимости
const cost = system.calculateCost();
console.log('Стоимость:', cost.totalCost);

// Генерация руководства
const guide = system.generateAssemblyGuide('ru');
console.log('Шаги:', guide.steps.length);
```

## 🧪 Тестирование

Система покрыта 24 тестами:
- ✅ Parameter management (4 теста)
- ✅ Tolerance management (3 теста)  
- ✅ Interactive editing state (3 теста)
- ✅ Cost calculation (4 теста)
- ✅ Assembly guide generation (3 теста)
- ✅ Version history management (5 теста)
- ✅ Parametric system integration (2 теста)

Все тесты проходят успешно:
```
PASS services/__tests__/ParametricSystem.test.ts
Test Suites: 1 passed, 1 total
Tests:       24 passed, 24 total
Time:        5.237 s
```

## 🔧 Интеграция с существующей архитектурой

Параметрическая система интегрирована с:
- **5-слойной архитектурой** проекта
- **CAD типизацией** из `CADTypes.ts`
- **Параметрами шкафа** из `ProductionArchitecture.ts`
- **Системой экспорта** для разных форматов

## 📈 Масштабируемость

- **Модульность**: Каждая функция выделена в отдельный класс
- **Расширяемость**: Добавление новых типов параметров и функций
- **Производительность**: Оптимизированные алгоритмы пересчета
- **Поддерживаемость**: Чистый код с документацией

## 🎯 Выводы

Параметрическая система v2.1 представляет собой полноценный инструмент для:
- Ускорения процесса проектирования
- Улучшения качества и консистентности проектов
- Минимизации ошибок через автоматическую валидацию
- Документации и контроль версий проекта

Система готовна для промышленного использования и интеграции с существующими инструментами CAD.