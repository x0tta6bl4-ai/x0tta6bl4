global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'cluster']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'default-receiver'
  
  routes:
    - match:
        severity: critical
      receiver: 'critical-receiver'

receivers:
  - name: 'default-receiver'
    # Placeholder for email/slack configs
    # email_configs:
    #   - to: 'admin@x0tta6bl4.dev'
    
  - name: 'critical-receiver'
    # Placeholder for pagerduty/opsgenie
    # pagerduty_configs:
    #   - service_key: <KEY>
