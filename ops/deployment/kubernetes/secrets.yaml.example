apiVersion: v1
kind: Secret
metadata:
  name: x0tta6bl4-secrets
  namespace: default
type: Opaque
stringData:
  # SPIFFE Configuration
  SPIFFE_ENDPOINT_SOCKET: "/run/spire/sockets/agent.sock"
  SPIFFE_TRUST_BUNDLE_PATH: "/var/run/secrets/spiffe/bundle.pem"
  
  # Database credentials (if using external DB)
  DB_HOST: "postgres.example.com"
  DB_USER: "x0tta6bl4"
  DB_PASSWORD: "CHANGE_ME"
  DB_NAME: "x0tta6bl4"
  
  # API Keys (if needed)
  API_KEY: "CHANGE_ME"
  
  # Encryption keys (if using external key management)
  ENCRYPTION_KEY: "CHANGE_ME"
  
---
# Instructions:
# 1. Copy this file: cp secrets.yaml.example secrets.yaml
# 2. Replace all "CHANGE_ME" values with actual secrets
# 3. Use kubectl create secret or kubectl apply -f secrets.yaml
# 4. NEVER commit secrets.yaml to git!

