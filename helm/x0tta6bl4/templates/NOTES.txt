╔══════════════════════════════════════════════════════════════╗
║       x0tta6bl4 Mesh Deployment Complete! 🚀                ║
╚══════════════════════════════════════════════════════════════╝

Thank you for installing {{ .Chart.Name }} v{{ .Chart.Version }}!

📊 STATUS CHECK:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "x0tta6bl4.name" . }}

🔍 LOGS:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "x0tta6bl4.name" . }} -f

📈 METRICS:
  # Port-forward Prometheus
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "x0tta6bl4.fullname" . }} {{ .Values.service.metricsPort }}:{{ .Values.service.metricsPort }}
  # Open: http://localhost:{{ .Values.service.metricsPort }}/metrics

🌐 ACCESS:
{{- if .Values.ingress.enabled }}
  URL: https://{{ (index .Values.ingress.hosts 0).host }}
{{- else }}
  # Port-forward service
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "x0tta6bl4.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  # Open: http://localhost:{{ .Values.service.port }}
{{- end }}

🧪 VERIFY COMPONENTS:
  # 1. Slot-Based Sync (MTTD target: ≤1900ms)
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "x0tta6bl4.name" . }} | grep MTTD

  # 2. Post-Quantum Crypto
  kubectl exec -n {{ .Release.Namespace }} deploy/{{ include "x0tta6bl4.fullname" . }} -- \
    curl -s localhost:{{ .Values.service.metricsPort }}/metrics | grep pq_crypto

  # 3. GraphSAGE AI (target: recall ≥92%)
  kubectl exec -n {{ .Release.Namespace }} deploy/{{ include "x0tta6bl4.fullname" . }} -- \
    curl -s localhost:{{ .Values.service.metricsPort }}/metrics | grep gnn_recall

  # 4. DAO Governance
  kubectl exec -n {{ .Release.Namespace }} deploy/{{ include "x0tta6bl4.fullname" . }} -- \
    curl -s localhost:{{ .Values.service.port }}/dao/status

{{- if .Values.chaosEngineering.enabled }}
🔥 CHAOS ENGINEERING ENABLED:
  Scenarios: pod-kill ({{ .Values.chaosEngineering.scenarios.podKill.percentage }}%), 
             network-delay ({{ .Values.chaosEngineering.scenarios.networkDelay.latency }}),
             partition ({{ .Values.chaosEngineering.scenarios.partition.percentage }}%)
{{- end }}

📚 DOCUMENTATION:
  https://github.com/x0tta6bl4/mesh-core

Need help? Open an issue: https://github.com/x0tta6bl4/mesh-core/issues
