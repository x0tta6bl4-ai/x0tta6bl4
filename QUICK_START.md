# 🚀 Quick Start Guide - x0tta6bl4 Bot

**Время:** 15-20 минут  
**Результат:** Работающий Telegram bot + Landing page

---

## ✅ Что уже готово:

- ✅ Telegram bot (`telegram_bot.py`)
- ✅ VPN config generator
- ✅ Database module
- ✅ QR code generator
- ✅ Landing page
- ✅ Deployment scripts

---

## 📋 Шаг 1: Создать Telegram бота (5 минут)

1. Открой Telegram
2. Найди @BotFather
3. Отправь `/newbot`
4. Введи имя: `x0tta6bl4 VPN`
5. Введи username: `x0tta6bl4_bot` (или другой свободный)
6. **Скопируй токен** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## 📋 Шаг 2: Запустить бота локально (5 минут)

```bash
cd /mnt/AC74CC2974CBF3DC

# Установить зависимости
pip3 install -r requirements_bot.txt

# Инициализировать базу данных
python3 -c "from database import init_database; init_database()"

# Запустить бота
export TELEGRAM_BOT_TOKEN="твой_токен_от_BotFather"
python3 telegram_bot.py
```

**Проверка:** Открой Telegram, найди своего бота, отправь `/start` — должен ответить!

---

## 📋 Шаг 3: Deploy бота на VPS (5 минут)

```bash
# На VPS (89.125.1.107)
cd /mnt/AC74CC2974CBF3DC

# Запустить deployment скрипт
sudo ./deploy_bot.sh
```

Скрипт автоматически:
- Установит зависимости
- Инициализирует базу данных
- Создаст systemd service
- Запустит бота

**Проверка:**
```bash
systemctl status x0tta6bl4-bot
journalctl -u x0tta6bl4-bot -f
```

---

## 📋 Шаг 4: Deploy Landing Page (5 минут)

```bash
# На твоём PC
cd /mnt/AC74CC2974CBF3DC

# Запустить deployment скрипт
./deploy_landing.sh
```

Скрипт автоматически:
- Загрузит landing page на VPS
- Настроит nginx (если установлен)
- Проверит доступность

**Проверка:** Открой http://89.125.1.107/landing.html в браузере

---

## 📋 Шаг 5: Первый пост (5 минут)

1. Открой `marketing_post_template.md`
2. Скопируй короткий вариант для Telegram
3. Найди 3-5 Telegram каналов про VPN/IT
4. Пости с ссылкой на бота

**Пример:**
```
🔥 Новый VPN который НЕ ПАДАЕТ
...
Попробуй 7 дней БЕСПЛАТНО:
👉 https://t.me/x0tta6bl4_bot?start=trial
```

---

## ✅ Checklist:

- [ ] Бот создан через @BotFather
- [ ] Бот отвечает на /start локально
- [ ] Бот задеплоен на VPS
- [ ] Landing page доступен
- [ ] Первый пост опубликован

---

## 🔧 Troubleshooting:

### Бот не отвечает:
```bash
# Проверить логи
journalctl -u x0tta6bl4-bot -n 50

# Проверить токен
echo $TELEGRAM_BOT_TOKEN

# Перезапустить
systemctl restart x0tta6bl4-bot
```

### Landing page не открывается:
```bash
# На VPS
sudo systemctl status nginx
curl http://localhost/landing.html
```

### База данных не работает:
```bash
# Переинициализировать
python3 -c "from database import init_database; init_database()"
```

---

## 📊 Следующие шаги:

1. **Мониторинг:** Следи за логами бота, отвечай на вопросы пользователей
2. **Улучшения:** Собирай feedback, улучшай бота
3. **Масштабирование:** Когда будет 10+ пользователей — подумай о x-ui API интеграции

---

## 💡 Полезные команды:

```bash
# Логи бота
journalctl -u x0tta6bl4-bot -f

# Статус бота
systemctl status x0tta6bl4-bot

# Перезапуск бота
systemctl restart x0tta6bl4-bot

# Проверить базу данных
python3 -c "from database import get_user_stats; print(get_user_stats())"
```

---

**Главное:** Не жди идеального продукта. Запускай с тем что есть. Первые пользователи дадут feedback для улучшений.

**Удачи! 🚀**

