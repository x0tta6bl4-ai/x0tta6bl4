# libx0t SDK 🛡️

**libx0t** — это Python SDK для создания квантово-устойчивых меш-сетей с функцией самовосстановления. Позволяет любому приложению получить уровень защиты Military-Grade PQC за считанные минуты.

## Ключевые особенности
- **Post-Quantum Security**: Встроенная поддержка алгоритмов NIST (ML-KEM/Kyber, ML-DSA/Dilithium).
- **Self-Healing Mesh**: Автоматическая перемаршрутизация трафика при падении узлов (MAPE-K loop).
- **Zero-Trust**: Интеграция со SPIFFE/SPIRE для идентификации сервисов.
- **eBPF-Powered**: Высокая производительность сетевого стека.

## Быстрый старт

### Установка (Alpha)
```bash
pip install libx0t
```

### Пример использования
```python
from libx0t import x0t
import asyncio

async def main():
    # Инициализация и подключение к меш-сети
    mesh = x0t.X0T()
    await mesh.connect(pqc_level="military")

    # Создание защищенного туннеля
    tunnel = mesh.create_tunnel("target-node-id")
    
    # Отправка данных
    await tunnel.send(b"Secure Message")

if __name__ == "__main__":
    asyncio.run(main())
```

## Архитектура
SDK скрывает сложность управления распределенным реестром (DAO), ротацией ключей и сетевыми протоколами (Batman-adv) за простым асинхронным API.

---
© 2026 x0tta6bl4 Project. DeepTech Platform for the Future.
