# 🔍 БЫСТРЫЙ СПРАВОЧНИК ПО ПОИСКУ И НАВИГАЦИИ

## Как использовать созданные индексы

### 1. AUDIT_INVENTORY_INDEX_REPORT.json
**Для:** Быстрого анализа размеров и типов файлов
```bash
# Просмотреть топ расширений
jq '.by_extension | to_entries | sort_by(.value.size) | reverse | .[0:10]' \
  AUDIT_INVENTORY_INDEX_REPORT.json

# Найти крупные файлы
jq '.largest_files[] | select(.size > 1000000000)' \
  AUDIT_INVENTORY_INDEX_REPORT.json
```

### 2. FULL_INVENTORY_INDEX.json
**Для:** Поиска конкретных файлов по пути или типу
```bash
# Найти все Python файлы размером > 5MB
jq '.file_index[] | select(.type == "Python" and .size > 5242880)' \
  FULL_INVENTORY_INDEX.json

# Найти файлы в конкретной папке
jq '.file_index[] | select(.dir | contains("src"))' \
  FULL_INVENTORY_INDEX.json
```

### 3. PROJECT_AUDIT_REPORT.json
**Для:** Проверки структуры проекта и критических файлов
```bash
# Проверить статус критических файлов
jq '.audit_sections.critical_files | to_entries | .[] | 
  select(.value.present == true) | .key' \
  PROJECT_AUDIT_REPORT.json
```

### 4. PROJECT_STRUCTURE_TREE.json
**Для:** Навигации по структуре проекта
- Иерархическое представление всех папок
- Размеры файлов на каждом уровне
- Максимум 3 уровня глубины для читаемости

### 5. SEARCH_INDEX.json
**Для:** Быстрого поиска по категориям

```bash
# Найти все Python модули с 'api' в названии
jq '.search_categories.python_modules[] | 
  select(.path | contains("api"))' \
  SEARCH_INDEX.json

# Найти все Markdown документы
jq '.search_categories.markdown_docs | length' \
  SEARCH_INDEX.json

# Список всех конфиг файлов
jq '.search_categories.config_files[] | .path' \
  SEARCH_INDEX.json
```

---

## 📊 КЛЮЧЕВЫЕ ЧИСЛА ДЛЯ ЗАПОМИНАНИЯ

| Метрика | Значение |
|---------|----------|
| Всего файлов | 415,937 |
| Python файлов | 107,790 |
| Markdown документов | 7,998 |
| Конфиг файлов | 201 |
| Data файлов | 5,737 |
| Всего размер | 251.5 GB |
| Крупнейший архив | 164 GB |
| Максимальная глубина папок | 15 уровней |

---

## 🎯 ТИПИЧНЫЕ ЗАДАЧИ

### Задача 1: Найти все Python файлы в src/
```bash
jq '.file_index[] | select(.type == "Python" and .dir | startswith("src"))' \
  FULL_INVENTORY_INDEX.json | jq -s 'length'
```

### Задача 2: Получить статистику по типам в x0tta6bl4_paradox_zone/
```bash
jq '.file_index[] | select(.dir | contains("x0tta6bl4_paradox_zone")) | .type' \
  FULL_INVENTORY_INDEX.json | jq -s 'group_by(.) | map({type: .[0], count: length})'
```

### Задача 3: Найти все файлы больше 100 MB
```bash
jq '.largest_files[] | select(.size > 104857600)' \
  AUDIT_INVENTORY_INDEX_REPORT.json
```

### Задача 4: Список всех Markdown документов
```bash
jq '.search_categories.markdown_docs | map(.path)' \
  SEARCH_INDEX.json
```

### Задача 5: Проверить, все ли критические файлы присутствуют
```bash
jq '.audit_sections.critical_files | 
  to_entries | 
  .[] | 
  select(.value.present == false) | 
  .key' \
  PROJECT_AUDIT_REPORT.json
```

---

## 🗂️ СТРУКТУРА ОСНОВНЫХ ПАПОК

```
/mnt/AC74CC2974CBF3DC/
├── src/                              # Исходный код проекта
├── archive/                          # Архивные данные и снимки
├── x0tta6bl4_paradox_zone/          # Основной рабочий проект
│   ├── x0tta6bl4/                   # Версия 1
│   ├── x0tta6bl4-unified/           # Унифицированная версия
│   ├── x0tta6bl4-next/              # Следующая версия
│   └── x0tta6bl4-registry/          # Реестр версий
├── личное/                           # Личные данные и медиа
├── .github/                          # Конфигурация GitHub
├── Makefile                          # Задачи сборки
├── pyproject.toml                    # Конфигурация Python
└── README.md                         # Документация проекта
```

---

## 🚀 БЫСТРЫЕ КОМАНДЫ

```bash
# Просмотреть дерево структуры
cat PROJECT_STRUCTURE_TREE.txt | head -100

# Подсчитать Python файлы
jq '[.file_index[] | select(.type == "Python")] | length' \
  FULL_INVENTORY_INDEX.json

# Найти файлы измененные после даты
jq '.file_index[] | select(.modified > "2026-01-01T00:00:00")' \
  FULL_INVENTORY_INDEX.json | jq -s 'length'

# Статистика по глубине папок
jq '.statistics.by_depth' FULL_INVENTORY_INDEX.json

# Размер по типам
jq '.statistics.by_type' AUDIT_INVENTORY_INDEX_REPORT.json
```

---

## 📝 ПРИМЕЧАНИЯ

⚠️ **Важно:**
- FULL_INVENTORY_INDEX.json большой файл (~200 MB), используйте jq для фильтрации
- Индексы обновлены: 12 января 2026 г. 02:06:52 UTC
- Некоторые файлы являются ссылками (symlinks) и могут быть недоступны

✅ **Преимущества JSON формата:**
- Легко парсить программно
- Поддержка структурированных данных
- Совместимость со всеми языками программирования
- Возможность использования jq для сложных запросов

---

## 🔧 ИНСТРУМЕНТЫ

### Установить jq (если не установлен)
```bash
# Ubuntu/Debian
sudo apt-get install jq

# macOS
brew install jq
```

### Примеры использования jq
```bash
# Красивый вывод JSON
jq '.' FULL_INVENTORY_INDEX.json | less

# Выбрать только определенные поля
jq '.file_index[] | {path: .path, size: .size, type: .type}' \
  FULL_INVENTORY_INDEX.json

# Сортировка по размеру
jq '.file_index | sort_by(.size) | reverse | .[0:20]' \
  FULL_INVENTORY_INDEX.json

# Подсчет уникальных типов
jq '.file_index | map(.type) | unique' \
  FULL_INVENTORY_INDEX.json
```

---

**Создано:** 12 января 2026 г.  
**Обновлено автоматически при каждом аудите.**

