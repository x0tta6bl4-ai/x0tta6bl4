{
  "title": "Federation Overview",
  "panels": [
    {
      "type": "timeseries",
      "title": "Cross-Cluster Latency (p99)",
      "targets": [{"expr": "histogram_quantile(0.99, rate(federation_cross_cluster_latency_seconds_bucket[5m]))"}]
    }
  ]
}