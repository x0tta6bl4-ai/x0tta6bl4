# 🔧 XRAY PORT CONFLICT - FIX GUIDE

**Дата:** 27 декабря 2025  
**Проблема:** X-UI не может запустить xray-core, порт 62789 занят systemd xray.service

---

## 🔍 ПРОБЛЕМА

```
ERROR - XRAY: Failed to start: app/proxyman/inbound: failed to listen TCP on 127.0.0.1:62789
bind: address already in use
```

**Причина:**
- systemd `xray.service` (PID 805) уже слушает порт 62789
- X-UI пытается запустить свой xray-core на том же порту
- Конфликт портов

---

## 🎯 РЕШЕНИЕ

### Вариант 1: Использовать только systemd xray (РЕКОМЕНДУЕТСЯ)

Xray уже работает через systemd. X-UI может управлять им через API.

**Шаги:**
1. Остановить попытки X-UI запускать свой xray-core
2. Настроить X-UI использовать существующий systemd xray
3. Или отключить автозапуск xray в X-UI

### Вариант 2: Использовать только X-UI xray

Если нужен X-UI для управления, остановить systemd xray.

**Шаги:**
1. Остановить systemd xray: `systemctl stop xray`
2. Отключить автозапуск: `systemctl disable xray`
3. Перезапустить X-UI

---

## 🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ

### Проверить текущую ситуацию:
```bash
# Проверить systemd xray
systemctl status xray

# Проверить X-UI
systemctl status x-ui

# Проверить порты
netstat -tulpn | grep 62789
```

### Решение 1: Отключить автозапуск xray в X-UI
```bash
# Войти в X-UI панель
# Настройки → Xray Settings
# Отключить "Auto start Xray"
# Или изменить порт 62789 на другой
```

### Решение 2: Остановить systemd xray (если используешь X-UI)
```bash
systemctl stop xray
systemctl disable xray
# Затем перезапустить X-UI
systemctl restart x-ui
```

---

## 📋 РЕКОМЕНДАЦИЯ

**Использовать systemd xray + X-UI только для управления**

1. systemd xray уже работает и стабилен
2. X-UI может управлять им через API
3. Не нужно запускать два процесса

**Настройка:**
- В X-UI панели отключить автозапуск xray
- Или изменить порт в X-UI на другой (например, 62790)

---

## ✅ ПРОВЕРКА

После исправления:
```bash
# Проверить что systemd xray работает
systemctl status xray

# Проверить что X-UI работает
systemctl status x-ui

# Проверить что порт 62789 свободен для X-UI (если нужно)
# Или что systemd xray использует его
netstat -tulpn | grep 62789
```

---

**Дата:** 27 декабря 2025  
**Статус:** 🔧 **FIX IN PROGRESS**

