# Исправление DNS для VPN

## ✅ Что исправлено на сервере:

1. **DNS серверы добавлены** - 8.8.8.8, 8.8.4.4, DoH
2. **Sniffing включен** - для правильной маршрутизации трафика
3. **Domain strategy** - UseIPv4 для стабильности

## ⚙️ Настройки клиента (важно!):

### Для v2rayN:

1. Откройте настройки сервера
2. Перейдите в **"Параметры"** → **"DNS"**
3. Установите:
   - **DNS сервер**: `8.8.8.8` или `1.1.1.1`
   - **Включить DNS через прокси**: ✅ ДА
   - **Включить sniffing**: ✅ ДА

### Альтернатива - системные настройки:

Если DNS все еще не работает через VPN:

1. **Проверьте системный DNS**:
   ```bash
   # Временно измените DNS
   sudo systemd-resolve --interface=lo --set-dns=8.8.8.8
   ```

2. **Или через NetworkManager**:
   - Откройте настройки сети
   - DNS: `8.8.8.8, 8.8.4.4`

## 🔍 Проверка работы DNS:

```bash
# Проверьте что DNS работает через VPN
dig @8.8.8.8 google.com
nslookup google.com 8.8.8.8
```

## 📝 Если все еще не работает:

1. **Перезапустите клиент VPN** полностью
2. **Проверьте что VPN активен** - должен быть зеленый индикатор
3. **Проверьте логи** - нет ли ошибок подключения
4. **Попробуйте другой DNS** - 1.1.1.1 вместо 8.8.8.8

---

**Сервер обновлен и работает!** Проблема была в отсутствии DNS настроек на сервере.

